---
phase: 02-classification-washing-and-ical-domain-logic
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/types/index.ts
  - src/domain/classify/classify-source-event.ts
  - src/domain/classify/index.ts
  - test/domain/classify.test.ts
autonomous: true
requirements:
  - SYNC-02
  - SYNC-03
  - SYNC-08
user_setup: []

must_haves:
  truths:
    - "ER-SENSITIVE always yields tier sensitive and propagation drop (D-01)"
    - "Empty categories yield untagged tier and busy propagation (D-03/SYNC-03)"
    - "Full propagation only when ER-PUBLIC alone with no ER-INTERNAL, ER-SENSITIVE, or non-ER tokens (D-02, SYNC-08)"
    - "Mixed ER-PUBLIC + ER-INTERNAL or unknown categories yield internal tier and busy (D-01, D-03)"
    - "ER tier tokens matched case-insensitively after trim (D-04)"
    - "Restrict-only policy enforced only in classifier — no washer in this plan (D-12)"
  artifacts:
    - path: src/domain/classify/classify-source-event.ts
      provides: "classifySourceEvent(SourceEvent) → Classification"
      exports: ["classifySourceEvent"]
    - path: src/domain/types/index.ts
      provides: "Classification, OutboundEvent, ProcessedSourceEvent types"
      contains: "export interface Classification"
    - path: test/domain/classify.test.ts
      provides: "Unit coverage for decision table and edge cases"
  key_links:
    - from: src/domain/classify/classify-source-event.ts
      to: src/domain/types/index.ts
      via: "reads SourceEvent.categories only"
      pattern: "event\\.categories"
    - from: test/domain/classify.test.ts
      to: src/domain/classify/index.js
      via: "in-memory SourceEvent literals — no node-ical"
      pattern: "classifySourceEvent"
---

<objective>
Deliver fail-closed `classifySourceEvent()` and shared domain types (`Classification`, `OutboundEvent`, `ProcessedSourceEvent`) using RED→GREEN TDD against the locked decision table.

Purpose: Security policy lives in one auditable classifier (SYNC-02, SYNC-08, SYNC-03); types unblock washer and orchestrator in plan 02-02.
Output: `src/domain/classify/` module, extended `domain/types`, passing `test/domain/classify.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-classification-washing-and-ical-domain-logic/02-CONTEXT.md
@.planning/phases/02-classification-washing-and-ical-domain-logic/02-RESEARCH.md
@.planning/phases/02-classification-washing-and-ical-domain-logic/02-PATTERNS.md
@.planning/phases/02-classification-washing-and-ical-domain-logic/02-VALIDATION.md

## Locked decisions (this plan)
- D-01, D-02, D-03, D-04: Classification decision table and case-insensitive ER tokens
- D-11: `OutboundEvent` and related types live in `domain/types/` (add now for wash plan)
- D-12: Restrict-only gate in classifier only

## Interfaces (existing)

From `src/domain/types/index.ts`:
```typescript
export type Tier = 'public' | 'internal' | 'sensitive' | 'untagged';
export type PropagationDecision = 'full' | 'busy' | 'drop';
export interface SourceEvent {
  uid: string;
  categories: string[];
  summary?: string;
  description?: string;
  location?: string;
  start: Date;
  end?: Date;
  recurrenceId?: Date;
  isRecurring: boolean;
}
```

Classifier must not import adapters or `node-ical`. Categories are already `string[]` from the adapter (D-04: normalize per token, do not re-split iCal).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Domain types and failing classify unit tests</name>
  <read_first>
    .planning/phases/02-classification-washing-and-ical-domain-logic/02-RESEARCH.md (Pattern 1 decision table)
    .planning/phases/02-classification-washing-and-ical-domain-logic/02-CONTEXT.md (D-01 through D-04, D-11, D-12)
    src/domain/types/index.ts
    test/smoke.test.ts
    vitest.config.ts
  </read_first>
  <files>src/domain/types/index.ts, test/domain/classify.test.ts</files>
  <behavior>
    - ER-SENSITIVE only → { tier: 'sensitive', propagation: 'drop' }
    - categories [] → { tier: 'untagged', propagation: 'busy' }
    - ['ER-PUBLIC'] only → { tier: 'public', propagation: 'full' }
    - ['ER-INTERNAL'] → { tier: 'internal', propagation: 'busy' }
    - ['ER-PUBLIC','ER-INTERNAL'] → { tier: 'internal', propagation: 'busy' } (strictest-wins, D-01)
    - ['ER-PUBLIC','CONFERENCE'] → { tier: 'internal', propagation: 'busy' } (non-ER blocks full, D-03)
    - ['WORK'] unknown-only → { tier: 'internal', propagation: 'busy' }
    - ['er-public'] case fold → full public path (D-04)
    - [' ER-PUBLIC '] trim → full public path
  </behavior>
  <action>
    Extend `src/domain/types/index.ts` per D-11:
    - `export interface Classification { tier: Tier; propagation: PropagationDecision }`
    - `export interface OutboundEvent` — same optional fields as `SourceEvent` where passthrough applies (uid, categories, summary, description, location, start, end, recurrenceId, isRecurring); busy output will use a subset in plan 02-02
    - `export interface ProcessedSourceEvent { tier: Tier; propagation: PropagationDecision; outbound: OutboundEvent | null }`

    Create `test/domain/classify.test.ts` importing `describe`, `it`, `expect` from `vitest` (globals: false). Build minimal `SourceEvent` factory helper inline or private function with required `uid`, `start`, `isRecurring` defaults.

    Import `classifySourceEvent` from `../../src/domain/classify/index.js` — expect import/compile failure until Task 2 creates the module; alternatively stub export that throws until GREEN.

    Encode every behavior case above as dedicated `it(...)` blocks. Run `npm test -- test/domain/classify.test.ts` and confirm tests fail (missing implementation or wrong results). Commit RED: `test(02-01): add failing tests for classifySourceEvent`.

    Do not implement classification logic in this task. Do not import `node-ical`.
  </action>
  <verify>
    <automated>npm test -- test/domain/classify.test.ts 2>&1 | grep -qE 'FAIL|Error|Cannot find|classifySourceEvent' && test -f test/domain/classify.test.ts && grep -q 'Classification' src/domain/types/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `Classification`, `OutboundEvent`, and `ProcessedSourceEvent` exported from `domain/types`
    - `classify.test.ts` exists with cases for sensitive, untagged, public-only, internal, mixed tags, unknown-only, and case/trim normalization
    - Test run fails before classifier implementation (RED gate)
    - No adapter or node-ical imports under `test/domain/`
  </acceptance_criteria>
  <done>RED: failing classify unit suite and type contracts committed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement classifySourceEvent and barrel export</name>
  <read_first>
    .planning/phases/02-classification-washing-and-ical-domain-logic/02-RESEARCH.md (Pattern 1 example code)
    .planning/phases/02-classification-washing-and-ical-domain-logic/02-PATTERNS.md (classify module section)
    test/domain/classify.test.ts
    src/domain/types/index.ts
  </read_first>
  <files>src/domain/classify/classify-source-event.ts, src/domain/classify/index.ts</files>
  <action>
    Create `src/domain/classify/classify-source-event.ts` implementing the RESEARCH Pattern 1 table exactly per D-01–D-04:
    - Normalize each category with `trim()`; drop empty strings from consideration
    - Case-insensitive ER constants: `er-public`, `er-internal`, `er-sensitive`
    - Check `ER-SENSITIVE` first → drop
    - Empty token list after trim → untagged + busy
    - `full` only when has public, no internal, no sensitive, and every token is a known ER token (no non-ER categories)
    - All other cases → internal + busy (including unknown-only and mixed ER)

    Export `classifySourceEvent(event: SourceEvent): Classification`. Do not throw on unknown categories — fail-closed to busy.

    Create `src/domain/classify/index.ts` re-exporting `classifySourceEvent` with `.js` extension in import path.

    Run `npm test -- test/domain/classify.test.ts` until green. Commit GREEN: `feat(02-01): implement classifySourceEvent`.

    Optional REFACTOR only if obvious (extract ER token helpers); must keep tests green.
  </action>
  <verify>
    <automated>npm test -- test/domain/classify.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `classifySourceEvent` is pure (no I/O, no node-ical)
    - All `classify.test.ts` cases pass
    - `propagation: 'full'` never returned when non-ER tokens present or ER-INTERNAL present (SYNC-08)
    - Sensitive path returns `drop`, not `busy` (D-01)
    - `npm run typecheck` passes
  </acceptance_criteria>
  <done>GREEN: classifier module complete and unit-tested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parsed categories → classifier | Untrusted strings from iCal; must not promote to full without public-only ER set |
| Classifier → downstream wash | `PropagationDecision` is the only disclosure signal (D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | classifySourceEvent | mitigate | Fail-closed decision table; unit tests for untagged, mixed, unknown (SYNC-03, SYNC-08) |
| T-02-02 | Information Disclosure | classifySourceEvent | mitigate | ER-SENSITIVE checked first → drop (D-01) |
| T-02-03 | Elevation of Privilege | classifySourceEvent | mitigate | Non-ER category blocks `full` even with ER-PUBLIC (D-03) |
| T-02-SC | Tampering | npm installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm test -- test/domain/classify.test.ts`
- `npm run typecheck`
- TDD gate: RED commit precedes GREEN commit for plan 02-01
</verification>

<success_criteria>
- Classifier matches locked decision table for all unit test cases
- SYNC-02, SYNC-03, SYNC-08 satisfied at unit level
- Types ready for wash and orchestrator in 02-02
</success_criteria>

<output>
Create `.planning/phases/02-classification-washing-and-ical-domain-logic/02-01-SUMMARY.md` when done
</output>
