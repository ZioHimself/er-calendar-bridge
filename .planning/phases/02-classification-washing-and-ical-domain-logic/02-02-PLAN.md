---
phase: 02-classification-washing-and-ical-domain-logic
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/domain/wash/wash-source-event.ts
  - src/domain/wash/index.ts
  - src/domain/process-source-event.ts
  - test/domain/wash.test.ts
autonomous: true
requirements:
  - SYNC-04
  - SYNC-09
  - SYNC-10
  - SYNC-08
user_setup: []

must_haves:
  truths:
    - "Busy propagation yields summary exactly 'Busy' (D-05)"
    - "Busy outbound built from whitelist only — no spread-copy from source (D-06, D-07)"
    - "Drop propagation yields null outbound (D-08)"
    - "Full propagation passthrough copies all current SourceEvent fields (D-09)"
    - "Busy outbound never includes categories, description, or location (SYNC-09, SYNC-10)"
    - "Recurrence sync metadata preserved on busy path when present (recurrenceId, isRecurring) (D-06)"
    - "processSourceEvent runs classify then wash without re-classifying in washer (D-10, D-12)"
    - "D-13: No attendee/organiser parsing extensions in Phase 2 — public passthrough uses existing SourceEvent fields only"
  artifacts:
    - path: src/domain/wash/wash-source-event.ts
      provides: "washSourceEvent(source, propagation)"
      exports: ["washSourceEvent"]
    - path: src/domain/process-source-event.ts
      provides: "processSourceEvent orchestrator"
      exports: ["processSourceEvent"]
    - path: test/domain/wash.test.ts
      provides: "Unit tests for busy/drop/full branches"
  key_links:
    - from: src/domain/process-source-event.ts
      to: src/domain/classify/classify-source-event.ts
      via: "classifySourceEvent then washSourceEvent"
      pattern: "classifySourceEvent"
    - from: src/domain/wash/wash-source-event.ts
      to: src/domain/types/index.ts
      via: "PropagationDecision drives branch — no tier re-check"
      pattern: "propagation === 'full'"
---

<objective>
Implement decision-driven washing and the `processSourceEvent` orchestrator with TDD, trusting classifier output only (D-12).

Purpose: Strip PII for non-public paths (SYNC-04, SYNC-09, SYNC-10) while preserving sync metadata for Phase 3 recurrence upserts.
Output: `src/domain/wash/`, `process-source-event.ts`, passing `test/domain/wash.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-classification-washing-and-ical-domain-logic/02-CONTEXT.md
@.planning/phases/02-classification-washing-and-ical-domain-logic/02-RESEARCH.md
@.planning/phases/02-classification-washing-and-ical-domain-logic/02-PATTERNS.md
@.planning/phases/02-classification-washing-and-ical-domain-logic/02-01-SUMMARY.md

## Locked decisions (this plan)
- D-05–D-09: Busy summary, whitelist, omit stripped keys, drop null, public passthrough
- D-10: Orchestrator `processSourceEvent` composes classify + wash
- D-12: Washer must not promote to full or re-run restrict-only logic
- D-13: No new attendee fields — whitelist prevents future PII leak on busy path

## Interfaces (from 02-01)

```typescript
export function classifySourceEvent(event: SourceEvent): Classification;

export function washSourceEvent(
  source: SourceEvent,
  propagation: PropagationDecision,
): OutboundEvent | null;

export function processSourceEvent(source: SourceEvent): ProcessedSourceEvent;
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Failing wash unit tests</name>
  <read_first>
    .planning/phases/02-classification-washing-and-ical-domain-logic/02-RESEARCH.md (Pattern 2, forbidden-key scan)
    .planning/phases/02-classification-washing-and-ical-domain-logic/02-CONTEXT.md (D-05 through D-09, D-12)
    src/domain/types/index.ts
    test/domain/classify.test.ts
  </read_first>
  <files>test/domain/wash.test.ts</files>
  <behavior>
    - propagation 'drop' → null regardless of rich source fields
    - propagation 'full' → outbound includes same summary, description, location, categories as source (passthrough D-09)
    - propagation 'busy' → summary 'Busy'; includes uid, start, end when source has end; omits categories, description, location
    - busy with isRecurring true on source → outbound has isRecurring true
    - busy with recurrenceId on source → outbound includes same recurrenceId instant
    - busy must not use spread of full source (assert forbidden keys absent even if source had them)
  </behavior>
  <action>
    Create `test/domain/wash.test.ts` with in-memory `SourceEvent` fixtures (rich internal event: summary, description, location, categories filled).

    Import `washSourceEvent` from `../../src/domain/wash/index.js`. Cover all behavior cases above including explicit checks that `'categories'`, `'description'`, `'location'` are not own properties on busy result (`expect('categories' in outbound).toBe(false)` or equivalent).

    Add describe block `passthrough` for full branch and `forbidden` for busy PII keys (maps to VALIDATION T-02-03).

    Run tests — must fail until Task 2. Commit RED: `test(02-02): add failing tests for washSourceEvent`.

    Do not implement washer yet. Do not add second restrict-only check in tests.
  </action>
  <verify>
    <automated>npm test -- test/domain/wash.test.ts; test $? -ne 0; test -f test/domain/wash.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Wash tests cover drop, full passthrough, busy whitelist, recurrenceId, and isRecurring
    - Forbidden-key assertions present for busy path
    - Tests fail before implementation (RED)
  </acceptance_criteria>
  <done>RED: wash unit suite committed and failing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Washer, orchestrator, and exports</name>
  <read_first>
    .planning/phases/02-classification-washing-and-ical-domain-logic/02-RESEARCH.md (Pattern 2 and Pattern 3)
    test/domain/wash.test.ts
    src/domain/classify/classify-source-event.ts
  </read_first>
  <files>src/domain/wash/wash-source-event.ts, src/domain/wash/index.ts, src/domain/process-source-event.ts</files>
  <action>
    Implement `washSourceEvent` per D-05–D-09:
    - `drop` → return `null`
    - `full` → return object copying all fields on `SourceEvent` (spread acceptable only on full branch)
    - `busy` → construct new object with only: `uid`, `start`, optional `end`, `summary: 'Busy'`, optional `recurrenceId`, optional `isRecurring: true` when `source.isRecurring` is true — never copy categories, description, location, or original summary

    Create `src/domain/wash/index.ts` barrel export.

    Create `src/domain/process-source-event.ts` per D-10:
    - `const { tier, propagation } = classifySourceEvent(source)`
    - `const outbound = washSourceEvent(source, propagation)`
    - return `{ tier, propagation, outbound }`

    Washer accepts `PropagationDecision` only — no access to re-derive tier from categories (D-12). Passing `busy` with a source that looks public must still busy-wash (unit test optional in wash.test.ts with forced propagation arg).

    Run `npm test -- test/domain/wash.test.ts` until green. Commit GREEN: `feat(02-02): implement wash and processSourceEvent`.

    Run `npm run typecheck`.
  </action>
  <verify>
    <automated>npm test -- test/domain/wash.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All wash unit tests pass
    - Busy path never exposes original summary or categories (SYNC-04, SYNC-09, SYNC-10)
    - `processSourceEvent` exported and composes classify + wash
    - No node-ical in domain modules
  </acceptance_criteria>
  <done>GREEN: wash module and orchestrator ready for fixture pipeline tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SourceEvent → washer | Source may contain PII; busy path must not echo fields outside whitelist |
| propagation argument | Must be treated as authoritative — no promotion in washer (D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | washSourceEvent busy branch | mitigate | Whitelist construction only; forbidden-key unit tests |
| T-02-04 | Elevation of Privilege | washSourceEvent | mitigate | No full branch unless propagation === 'full'; test forced busy on rich source |
| T-02-05 | Information Disclosure | washSourceEvent full branch | accept | Intended disclosure for ER-PUBLIC-only path decided upstream |
| T-02-SC | Tampering | npm installs | accept | No new packages this plan |
</threat_model>

<verification>
- `npm test -- test/domain/wash.test.ts`
- `npm run typecheck`
- TDD RED then GREEN commits for plan 02-02
</verification>

<success_criteria>
- SYNC-04, SYNC-09, SYNC-10 satisfied at unit level
- Orchestrator ready for fixture integration in 02-03
</success_criteria>

<output>
Create `.planning/phases/02-classification-washing-and-ical-domain-logic/02-02-SUMMARY.md` when done
</output>
