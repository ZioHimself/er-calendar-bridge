---
phase: 02-classification-washing-and-ical-domain-logic
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - test/helpers/assert-sidecar.ts
  - test/domain/pipeline-fixtures.test.ts
  - test/fixtures/internal/team-sync.expected.json
  - test/fixtures/untagged/unclassified.expected.json
  - test/fixtures/recurrence/weekly-series.expected.json
  - test/fixtures/recurrence/modified-instance.expected.json
  - test/fixtures/recurrence/deleted-instance.expected.json
autonomous: true
requirements:
  - TEST-04
  - SYNC-02
  - SYNC-03
  - SYNC-04
  - SYNC-08
  - SYNC-09
  - SYNC-10
user_setup: []

must_haves:
  truths:
    - "parse → processSourceEvent → assertProcessed matches each tier fixture sidecar (D-14)"
    - "Busy-tier sidecars contain full expected washed OutboundEvent objects (D-15)"
    - "Recurrence fixtures run full pipeline with tier, propagation, and washed assertions — not UID-only (D-15)"
    - "Public fixture keeps washed null; outbound equals passthrough of parsed source (D-09)"
    - "Sensitive fixture yields outbound null with propagation drop"
    - "assertProcessed normalizes ISO date strings from JSON to Date for comparison (D-16)"
  artifacts:
    - path: test/helpers/assert-sidecar.ts
      provides: "assertProcessed, assertBusyOutbound"
      exports: ["assertTier", "assertProcessed"]
    - path: test/domain/pipeline-fixtures.test.ts
      provides: "End-to-end fixture pipeline coverage"
    - path: test/fixtures/internal/team-sync.expected.json
      provides: "Populated washed busy object"
      contains: "\"summary\": \"Busy\""
  key_links:
    - from: test/domain/pipeline-fixtures.test.ts
      to: src/domain/process-source-event.ts
      via: "processSourceEvent(parseIcs(ics))"
      pattern: "processSourceEvent"
    - from: test/helpers/assert-sidecar.ts
      to: test/helpers/load-fixture.ts
      via: "FixtureExpected.washed comparison"
      pattern: "expected\\.washed"
---

<objective>
Complete TEST-04 by extending assertion helpers, populating busy-tier sidecar `washed` objects, and proving the full parse → classify → wash pipeline on all fixtures including recurrence.

Purpose: Fixture-driven proof that classification and washing match operator expectations before CalDAV/Google integration in Phase 3.
Output: `assertProcessed`, updated `.expected.json` files, `pipeline-fixtures.test.ts`, full `npm test` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-classification-washing-and-ical-domain-logic/02-CONTEXT.md
@.planning/phases/02-classification-washing-and-ical-domain-logic/02-RESEARCH.md
@.planning/phases/02-classification-washing-and-ical-domain-logic/02-PATTERNS.md
@.planning/phases/02-classification-washing-and-ical-domain-logic/02-02-SUMMARY.md

## Verified washed instants (adapter parse — use in sidecars)

| Fixture | washed fields (UTC ISO) |
|---------|-------------------------|
| internal/team-sync | start `2026-09-16T08:00:00.000Z`, end `2026-09-16T08:30:00.000Z`, uid `team-sync-2026@er.example` |
| untagged/unclassified | start `2026-09-19T07:00:00.000Z`, end `2026-09-19T08:00:00.000Z`, uid `unclassified-2026@er.example` |
| recurrence/weekly-series | start `2026-09-01T07:00:00.000Z`, end `2026-09-01T07:30:00.000Z`, `isRecurring: true` |
| recurrence/modified-instance | recurrenceId `2026-09-15T07:00:00.000Z`, start/end as parsed, summary Busy |
| recurrence/deleted-instance | `isRecurring: true`, start `2026-09-01T13:00:00.000Z`, end `2026-09-01T14:00:00.000Z` |

Public and sensitive sidecars keep `"washed": null`; tests assert `outbound` directly for full passthrough and null drop.

## Existing smoke harness

`test/smoke.test.ts` remains parse-only smoke; pipeline depth lives in `pipeline-fixtures.test.ts` per D-14.
</context>

<tasks>

<task type="auto">
  <name>Task 1: assertProcessed helper and busy sidecar population</name>
  <read_first>
    test/helpers/assert-sidecar.ts
    test/helpers/load-fixture.ts
    .planning/phases/02-classification-washing-and-ical-domain-logic/02-RESEARCH.md (Pattern 4, Pitfall 5, forbidden-key scan)
    .planning/phases/02-classification-washing-and-ical-domain-logic/02-CONTEXT.md (D-15, D-16)
  </read_first>
  <files>
    test/helpers/assert-sidecar.ts
    test/fixtures/internal/team-sync.expected.json
    test/fixtures/untagged/unclassified.expected.json
    test/fixtures/recurrence/weekly-series.expected.json
    test/fixtures/recurrence/modified-instance.expected.json
    test/fixtures/recurrence/deleted-instance.expected.json
  </files>
  <action>
    Extend `test/helpers/assert-sidecar.ts` per D-16:
    - Import `ProcessedSourceEvent`, `OutboundEvent` from domain types
    - Export `assertBusyOutbound(outbound: OutboundEvent)` — throw if `summary !== 'Busy'` or if `categories`, `description`, or `location` are own properties
    - Export `assertProcessed(result: ProcessedSourceEvent, expected: FixtureExpected)`:
      - Assert `result.tier === expected.tier` and `result.propagation === expected.propagation`
      - When `expected.propagation === 'drop'`, assert `result.outbound === null`
      - When `expected.propagation === 'full'` and `expected.washed === null`, assert outbound is non-null and matches parsed fields relevant to passthrough (at minimum uid and categories present on outbound)
      - When `expected.washed` is non-null, deep-compare `result.outbound` to expected.washed after normalizing known date fields (`start`, `end`, `recurrenceId`) from JSON strings to `Date` objects
    - Keep `assertTier` for backward compatibility; remove or resolve the Phase 2 TODO comment once `assertProcessed` exists

    Populate `washed` in busy-tier sidecars per D-15 using verified instants in context (summary always `"Busy"`, no categories/description/location keys). Example `internal/team-sync.expected.json` washed object:
    `{"uid":"team-sync-2026@er.example","start":"2026-09-16T08:00:00.000Z","end":"2026-09-16T08:30:00.000Z","summary":"Busy"}`

    Re-run `npx tsx` parse one-liner on each fixture if dates drift after edits.

    Do not change `.ics` files unless parse output genuinely differs.
  </action>
  <verify>
    <automated>grep -q 'assertProcessed' test/helpers/assert-sidecar.ts && node -e "const j=require('./test/fixtures/internal/team-sync.expected.json'); if(!j.washed||j.washed.summary!=='Busy') process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `assertProcessed` and `assertBusyOutbound` exported
    - Five busy/recurrence sidecars have non-null `washed` with correct ISO instants and Busy summary
    - public/sensitive expected files unchanged with `washed: null`
    - Date normalization handles JSON string vs Date mismatch (Pitfall 5)
  </acceptance_criteria>
  <done>Helpers and sidecar expectations ready for pipeline tests</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline fixture tests and phase validation gate</name>
  <read_first>
    test/smoke.test.ts
    test/helpers/parse-ics.ts
    src/domain/process-source-event.ts
    .planning/phases/02-classification-washing-and-ical-domain-logic/02-VALIDATION.md
  </read_first>
  <files>test/domain/pipeline-fixtures.test.ts</files>
  <action>
    Create `test/domain/pipeline-fixtures.test.ts`:
    - Import `loadFixture`, `parseIcs`, `assertProcessed` (and `assertBusyOutbound` where useful)
    - Import `processSourceEvent` from `../../src/domain/process-source-event.js`
    - Parameterize over tier fixtures: public/board-meeting, internal/team-sync, sensitive/hr-review, untagged/unclassified — each loads fixture, `processSourceEvent(parseIcs(ics))`, `assertProcessed(result, expected)`
    - Add recurrence loop: weekly-series, modified-instance, deleted-instance under `recurrence/` with full pipeline assertions (replace UID-only smoke coverage for classification behavior per D-15)
    - For sensitive hr-review: assert `outbound === null` via sidecar propagation drop
    - For public: assert outbound retains parsed summary (not washed to Busy)

    Run full suite and quality gates per 02-VALIDATION Wave 3: `npm test && npm run typecheck && npm run lint`.

    Leave `test/smoke.test.ts` as lightweight parse smoke unless a duplicate failure occurs — do not delete smoke tests.

    Optional per discretion: add one synthesized classify edge case file only if not already in `classify.test.ts` — not required if 02-01 covered mixed tags.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline-fixtures.test.ts` covers all seven fixture pairs with processSourceEvent
    - Recurrence fixtures assert tier, propagation, and washed (not uid-only)
    - Full `npm test` passes (TEST-04)
    - lint and typecheck pass
  </acceptance_criteria>
  <done>Phase 2 domain pipeline proven against fixture library</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture sidecars → assertions | Incorrect washed JSON could hide PII leaks — instants must match adapter parse |
| Pipeline tests → production path | Same `processSourceEvent` entry Phase 3 will use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | pipeline-fixtures.test.ts | mitigate | Full washed compare + assertBusyOutbound on busy tiers |
| T-02-05 | Information Disclosure | recurrence busy metadata | mitigate | Sidecars require recurrenceId/isRecurring where applicable |
| T-02-SC | Tampering | npm installs | accept | No installs |
</threat_model>

<verification>
- `npm test`
- `npm run typecheck`
- `npm run lint`
- All SYNC-02–04, SYNC-08–10 and TEST-04 mapped in 02-VALIDATION.md turn green
</verification>

<success_criteria>
- TEST-04 complete: classify, wash, and pipeline covered without network
- ROADMAP Phase 2 success criteria 1–5 achievable via fixture assertions
- Ready for `/gsd:verify-work` on Phase 2
</success_criteria>

<output>
Create `.planning/phases/02-classification-washing-and-ical-domain-logic/02-03-SUMMARY.md` when done
</output>
