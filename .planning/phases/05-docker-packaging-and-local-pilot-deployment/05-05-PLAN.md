---
phase: 05-docker-packaging-and-local-pilot-deployment
plan: 05
type: execute
wave: 3
depends_on:
  - 05-04
files_modified:
  - README.md
  - docs/runbooks/docker-pilot.md
  - docs/runbooks/offboarding.md
  - .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-VALIDATION.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - SYNC-17
  - SYNC-18
  - SEC-03
  - OPS-02
  - OPS-04
  - TEST-05
user_setup: []

must_haves:
  truths:
    - "Operator can follow docs to docker compose up pilot with GHCR IMAGE and secrets/operator files (SYNC-17, OPS-01)"
    - "Google re-auth on host documented: update secret file, restart container (D-07, D-09, SEC-03)"
    - "Host migration steps: copy data/ and secrets/, adjust .env (D-21, D-22, OPS-02)"
    - "Offboarding runbook: revoke CalDAV, compose down, optional bridge.db backup — no bulk Google delete (D-24, D-25, SYNC-18)"
    - "Vault/OpenBao noted as follow-up; file secrets for v1.0 (D-23)"
    - "Phase validation doc lists automated gates including docker-build (TEST-05)"
  artifacts:
    - path: docs/runbooks/docker-pilot.md
      provides: Onboarding, IMAGE pull, migration, Google OAuth host flow
    - path: docs/runbooks/offboarding.md
      provides: SYNC-18 manual procedure
    - path: README.md
      provides: Links to runbooks and five-job CI
    - path: .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-VALIDATION.md
      provides: Updated task map and phase gate
  key_links:
    - from: README.md
      to: docs/runbooks/docker-pilot.md
      via: markdown links
      pattern: docker-pilot
    - from: docs/runbooks/offboarding.md
      to: compose.yaml
      via: compose down service name bridge-operator
      pattern: compose down
---

<objective>
Document operator Docker pilot, Google OAuth host re-auth, host migration, and offboarding; align README and phase validation gate with five-job CI and v1.0 milestone.

Purpose: SYNC-18 runbook-only; OPS-02 portable deployment; D-07–D-09, D-20–D-25; TEST-05 phase gate documentation.
Output: Runbooks, README updates, 05-VALIDATION.md and ROADMAP phase 5 plan list finalized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-docker-packaging-and-local-pilot-deployment/05-CONTEXT.md (all D-07–D-25)
@.planning/phases/05-docker-packaging-and-local-pilot-deployment/05-VALIDATION.md
@.planning/ROADMAP.md (Phase 5 success criteria — update named volume / no-push drift per RESEARCH)
@compose.yaml
@secrets/operator/README.md
@README.md

</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker pilot and migration runbook</name>
  <read_first>
    .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-CONTEXT.md (D-05–D-12, D-20–D-23)
    compose.yaml
    secrets/operator/README.md
    .env.example
  </read_first>
  <files>docs/runbooks/docker-pilot.md</files>
  <action>
    Create docs/runbooks/docker-pilot.md covering end-to-end v1.0 operator pilot per ROADMAP success criterion 5:

    Prerequisites: Docker Compose V2, encrypted disk, GitHub GHCR pull access (public package per RESEARCH A2).

    Onboarding: copy .env.example → .env; set IMAGE=ghcr.io/europeanresolve/er-calendar-bridge:1.0.0; fill non-secret vars; create secrets/operator/* files per README (D-01, D-02); mkdir -p data/operator; docker compose pull && docker compose up -d (D-12).

    Runtime: default sync --watch; SYNC_INTERVAL_SECONDS from .env (D-13); logs via docker logs JSON (D-15); no inbound ports (D-11).

    Google OAuth (D-05–D-09, SEC-03, D-08): personal account refresh token in secret file; calendar-only scope unchanged; host re-auth procedure — run bridge CLI on host outside container, write google_refresh_token file, docker compose restart; document token revocation and third-party access in Google account settings.

    Host migration (D-21, D-22, OPS-02): copy data/&lt;member&gt; and secrets/&lt;member&gt;; same compose.yaml; server .env for non-secrets; no hardcoded paths in app (reference OPS-02).

    Vault follow-up (D-23): file-based secrets for Phase 5 only.

    Multi-member: copy service block + secrets/data dirs (D-04).

    Link to audit list CLI from Phase 4 for withhold inspection.
  </action>
  <verify>
    <automated>test -f docs/runbooks/docker-pilot.md && grep -q 'google_refresh_token' docs/runbooks/docker-pilot.md && grep -q 'IMAGE' docs/runbooks/docker-pilot.md && grep -q 'migration' docs/runbooks/docker-pilot.md</automated>
  </verify>
  <acceptance_criteria>
    - Runbook includes compose up steps and secret file layout
    - Host re-auth and offline refresh token guidance present (D-09)
    - Migration section lists data/ and secrets/ copy (D-21)
    - No instruction to bake secrets into image
  </acceptance_criteria>
  <done>docker-pilot runbook complete</done>
</task>

<task type="auto">
  <name>Task 2: Offboarding runbook (SYNC-18)</name>
  <read_first>
    .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-CONTEXT.md (D-24, D-25)
    .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-RESEARCH.md (offboarding pattern)
  </read_first>
  <files>docs/runbooks/offboarding.md</files>
  <action>
    Create docs/runbooks/offboarding.md per D-24/D-25:

    Steps: revoke mailbox.org app-specific password; optional revoke Google third-party app access; docker compose down bridge-operator (or full stack); remove container; optional backup bridge.db from ./data/operator before rm data dir for audit retention (D-25).

    Explicitly state v1.0 does NOT automate deletion of propagated Google events — manual cleanup in Google Calendar if desired (deferred bulk CLI).

    Reference SYNC-18 requirement ID for traceability.
  </action>
  <verify>
    <automated>test -f docs/runbooks/offboarding.md && grep -q 'compose down' docs/runbooks/offboarding.md && grep -q 'bridge.db' docs/runbooks/offboarding.md && grep -Eiq 'no automated|does not automate|manual' docs/runbooks/offboarding.md</automated>
  </verify>
  <acceptance_criteria>
    - Revoke CalDAV credential step documented
    - compose down documented
    - Optional bridge.db backup before data deletion (D-25)
    - No automated mass-delete of Google events (D-24)
  </acceptance_criteria>
  <done>Offboarding runbook satisfies SYNC-18</done>
</task>

<task type="auto">
  <name>Task 3: README, validation gate, ROADMAP alignment</name>
  <read_first>
    README.md
    .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-VALIDATION.md
    .planning/ROADMAP.md (Phase 5 section)
    .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-RESEARCH.md (ROADMAP drift note)
  </read_first>
  <files>README.md, .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-VALIDATION.md, .planning/ROADMAP.md</files>
  <action>
    Update README Deployment section: five CI jobs including docker-build; link docs/runbooks/docker-pilot.md and offboarding.md; document local gate npm test && lint && typecheck && build && optional docker build; GHCR IMAGE pull pilot (D-12).

    Update 05-VALIDATION.md: mark wave gaps resolved with plan task IDs (05-01 through 05-05); set nyquist_compliant true when map complete; phase gate = full npm suite + CI docker-build on main; per-task verification table filled.

    Update ROADMAP Phase 5: Plans 5/5; list all PLAN files; adjust success criteria text if still mentioning named volume only or no registry push — align with CONTEXT bind mount and GHCR push (per RESEARCH state of the art).

    Do not change application code in this task unless README-only.
  </action>
  <verify>
    <automated>grep -q 'docker-build' README.md && grep -q 'docker-pilot' README.md && grep -q '05-01-PLAN' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - README references runbooks and five-job CI
    - 05-VALIDATION.md phase gate mentions docker-build
    - ROADMAP Phase 5 plans list matches created PLAN files
    - ROADMAP success criteria reflect bind mount + GHCR (CONTEXT wins over stale ROADMAP lines)
  </acceptance_criteria>
  <done>Docs and planning artifacts aligned for verify-work</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator runbooks | Human executes revoke/backup steps |
| Google account settings | Third-party access revocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Information Disclosure | Secrets in README examples | mitigate | Placeholders only; point to gitignored files |
| T-05-17 | Denial of Service | Accidental data wipe on offboard | mitigate | Optional bridge.db backup step (D-25) |
| T-05-18 | Information Disclosure | Stale Google tokens after offboard | mitigate | Document revoke third-party access |
| T-05-19 | Repudiation | No audit after offboard | accept | Optional db backup; audit in SQLite until deleted |
</threat_model>

<verification>
Runbooks exist; README links; 05-VALIDATION.md and ROADMAP updated; manual UAT checklist in docker-pilot for operator encrypted laptop pilot.
</verification>

<success_criteria>
Operator documentation complete for pilot, migration, re-auth, offboarding; phase validation and ROADMAP reflect executed plan set.
</success_criteria>

<output>
Create `.planning/phases/05-docker-packaging-and-local-pilot-deployment/05-05-SUMMARY.md` when done
</output>
