---
phase: 05-docker-packaging-and-local-pilot-deployment
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - Dockerfile
  - .dockerignore
autonomous: true
requirements:
  - SYNC-17
  - SEC-02
  - TEST-05
user_setup: []

must_haves:
  truths:
    - "docker build succeeds without provider credentials in build context (TEST-05)"
    - "Container runs as non-root node user (D-16)"
    - "Default CMD runs sync --watch matching host CLI (D-13)"
    - "D-15: Container logs structured JSON to stdout via existing pino logger (no file logging in image)"
    - "Image based on node:24-alpine multi-stage build (D-17)"
    - "better-sqlite3 loads in built image (RESEARCH Pitfall 1)"
  artifacts:
    - path: Dockerfile
      provides: Multi-stage production image
      contains: "USER node"
    - path: .dockerignore
      provides: Excludes secrets, data, tests from context
      contains: "secrets"
  key_links:
    - from: Dockerfile
      to: dist/index.js
      via: CMD node dist/index.js sync --watch
      pattern: sync.*--watch
    - from: .dockerignore
      to: build context
      via: exclude secrets/ data/ .env
      pattern: secrets
---

<objective>
Add production multi-stage Dockerfile and .dockerignore per spike findings; verify image builds and native SQLite module loads.

Purpose: SYNC-17 image artifact; SEC-02 keeps secrets out of layers; D-13/D-16/D-17 runtime contract.
Output: `Dockerfile`, `.dockerignore`, local `docker build` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-docker-packaging-and-local-pilot-deployment/05-SPIKE-ALPINE.md
@.planning/phases/05-docker-packaging-and-local-pilot-deployment/05-RESEARCH.md (Pattern 2)
@.planning/phases/05-docker-packaging-and-local-pilot-deployment/05-PATTERNS.md (Dockerfile section)
@package.json
@tsconfig.json

<interfaces>
Build: npm ci, npm run build → dist/
Runtime entry: node dist/index.js (src/index.ts main)
Engines: node 24.x
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-stage Dockerfile and .dockerignore</name>
  <read_first>
    .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-SPIKE-ALPINE.md
    .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-RESEARCH.md (Pattern 2)
    package.json
    tsconfig.json
  </read_first>
  <files>Dockerfile, .dockerignore</files>
  <action>
    Create .dockerignore excluding: .git, node_modules, dist, test, .planning, secrets, data, .env, .vitest, coverage artifacts — per RESEARCH (no secrets in context).

    Create Dockerfile per D-17: build stage FROM node:24-alpine with python3 make g++ (or spike doc packages); WORKDIR /app; COPY package.json package-lock.json; npm ci; COPY tsconfig.json src; npm run build; production node_modules per spike (prune --omit=dev or ci --omit=dev with build deps).

    Runtime stage FROM node:24-alpine; LABEL org.opencontainers.image.source=https://github.com/europeanresolve/er-calendar-bridge; WORKDIR /app; chown node:node; USER node; COPY --chown=node:node package.json, node_modules, dist from build; ENV NODE_ENV=production; CMD ["node", "dist/index.js", "sync", "--watch"].

    Never use ARG/ENV for credentials (D-02). No EXPOSE or ports (D-11 alignment for image-only).
  </action>
  <verify>
    <automated>docker build -t er-calendar-bridge:local .</automated>
  </verify>
  <acceptance_criteria>
    - docker build exits 0 on developer machine or CI runner with Docker
    - Dockerfile contains USER node and multi-stage FROM lines
    - .dockerignore lists secrets/ and data/
  </acceptance_criteria>
  <done>Image builds locally without secret files in context</done>
</task>

<task type="auto">
  <name>Task 2: Runtime smoke — better-sqlite3 and CLI usage</name>
  <read_first>
    src/index.ts (printUsage, main without config)
    .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-RESEARCH.md (CI image smoke)
  </read_first>
  <files>Dockerfile</files>
  <action>
    After build, run: docker run --rm er-calendar-bridge:local node -e "require('better-sqlite3')" — must exit 0.

    Run: docker run --rm er-calendar-bridge:local node dist/index.js — expect usage text and exit 0 without MAILBOX_* env (may exit 0 on empty argv per main).

    If usage requires no loadConfig on empty argv, use node dist/index.js with no args; if loadConfig runs only on subcommands, verify printUsage path matches index.ts behavior.

    Document any CMD override needed in compose (Plan 05-03) if default CMD already watch — smoke uses explicit node -e and bare entry for TEST-05 credential-free checks.
  </action>
  <verify>
    <automated>docker run --rm er-calendar-bridge:local node -e "require('better-sqlite3')" && docker run --rm er-calendar-bridge:local node dist/index.js 2>&1 | grep -q Usage</automated>
  </verify>
  <acceptance_criteria>
    - better-sqlite3 loads inside image
    - CLI prints usage without provider env vars
    - No secret env vars passed to docker run smoke commands
  </acceptance_criteria>
  <done>Runtime smoke proves native module and CLI entry</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker build context | Must not include .env or secrets/ |
| Image layers | No credential ENV/ARG |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | Secrets in image layers | mitigate | .dockerignore; no ARG secrets; runtime mounts only |
| T-05-06 | Elevation | Container runs as root | mitigate | USER node in Dockerfile (D-16) |
| T-05-07 | Denial of Service | Broken native module | mitigate | Spike-guided build; smoke require('better-sqlite3') |
| T-05-SC | Tampering | Base image supply chain | mitigate | Official node:24-alpine; pin by digest optional discretion |
</threat_model>

<verification>
docker build -t er-calendar-bridge:local . && docker run --rm er-calendar-bridge:local node -e "require('better-sqlite3')"
</verification>

<success_criteria>
Production Dockerfile builds; non-root runtime; better-sqlite3 and usage smoke pass without credentials.
</success_criteria>

<output>
Create `.planning/phases/05-docker-packaging-and-local-pilot-deployment/05-02-SUMMARY.md` when done
</output>
