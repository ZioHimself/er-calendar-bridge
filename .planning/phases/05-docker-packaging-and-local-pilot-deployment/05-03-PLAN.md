---
phase: 05-docker-packaging-and-local-pilot-deployment
plan: 03
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - compose.yaml
  - compose.notify.yaml
  - .env.example
  - secrets/operator/.gitkeep
  - secrets/operator/README.md
autonomous: true
requirements:
  - SYNC-17
  - SEC-02
  - SEC-04
  - OPS-01
user_setup: []

must_haves:
  truths:
    - "docker compose config validates pilot stack (SYNC-17)"
    - "Single compose.yaml with bridge-operator service and copy-paste pattern for more members (D-04)"
    - "Secrets via Compose secrets: one file per credential under secrets/operator/ (D-01)"
    - "Bind mount ./data/operator → DATA_DIR; no published ports (D-10, D-11)"
    - "Image from ${IMAGE} env pointing at GHCR semver tag (D-12)"
    - "restart: unless-stopped and sync --watch with SYNC_INTERVAL_SECONDS from env (D-13, D-14)"
  artifacts:
    - path: compose.yaml
      provides: Pilot operator service
      contains: "secrets:"
    - path: .env.example
      provides: IMAGE, DATA_DIR, non-secret config
      contains: "IMAGE="
    - path: secrets/operator/README.md
      provides: Required secret filenames for onboarding
  key_links:
    - from: compose.yaml
      to: /run/secrets
      via: secrets file mounts
      pattern: mailbox_app_password
    - from: compose.yaml
      to: src/secrets/resolve-env.ts
      via: compose secret names match COMPOSE_SECRET_TO_ENV
      pattern: google_refresh_token
---

<objective>
Add Docker Compose pilot stack, document per-member secret files, and extend .env.example for GHCR image pull and container paths.

Purpose: SYNC-17 orchestration; SEC-04 per-member isolation; OPS-01 single-account pilot layout (D-01–D-12, D-14).
Output: `compose.yaml`, updated `.env.example`, `secrets/operator/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-docker-packaging-and-local-pilot-deployment/05-CONTEXT.md (D-01–D-14)
@.planning/phases/05-docker-packaging-and-local-pilot-deployment/05-RESEARCH.md (Pattern 1)
@.planning/phases/05-docker-packaging-and-local-pilot-deployment/05-PATTERNS.md (compose.yaml greenfield, .env.example exact)
@src/secrets/resolve-env.ts
@.env.example

<interfaces>
Compose secret names (must match resolve-env mapping): mailbox_app_password, google_client_id, google_client_secret, google_refresh_token. SMTP: smtp_password only in optional overlay `compose.notify.yaml` when NOTIFY_OWNER_EMAIL is set (not in base compose — avoids missing-file `docker compose config` failures).

Container env: DATA_DIR=/data, SYNC_INTERVAL_SECONDS, LOG_LEVEL, mailbox/google URLs and usernames via env_file .env (non-secrets only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: compose.yaml pilot operator service</name>
  <read_first>
    .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-RESEARCH.md (Pattern 1 YAML)
    src/secrets/resolve-env.ts (compose name mapping)
    src/config/env.ts (DATA_DIR, SYNC_INTERVAL_SECONDS)
  </read_first>
  <files>compose.yaml, compose.notify.yaml</files>
  <action>
    Create compose.yaml at repo root per D-04: service bridge-operator with image: ${IMAGE}, restart: unless-stopped, env_file: .env, environment DATA_DIR: /data (or document in .env), volumes ./data/operator:/data, no ports: block (D-11).

    Declare top-level secrets with file paths under ./secrets/operator/ for mailbox_app_password, google_client_id, google_client_secret, google_refresh_token only (required pilot credentials).

    Add `compose.notify.yaml` overlay: extends bridge-operator secrets with smtp_password file; document deploy as `docker compose -f compose.yaml -f compose.notify.yaml up -d` when withhold SMTP notify is enabled. Base `docker compose config` must pass with only the four required secret files present.

    Attach secrets list to bridge-operator. command may omit if image CMD is sync --watch (D-13); if overriding, use ["node","dist/index.js","sync","--watch"].

    Add commented second-service stub showing copy-paste for another member (different secrets dir + data mount) per D-01 onboarding pattern.

    Per D-05/D-06: no credentials in compose environment: section — only secret mounts.
  </action>
  <verify>
    <automated>docker compose config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - docker compose config exits 0 (use dummy IMAGE=ghcr.io/europeanresolve/er-calendar-bridge:1.0.0 in env or inline export)
    - compose.yaml has no ports: mapping
    - secrets: references files under ./secrets/operator/
    - restart policy unless-stopped present
  </acceptance_criteria>
  <done>Valid Compose file for operator pilot</done>
</task>

<task type="auto">
  <name>Task 2: .env.example and secrets onboarding docs</name>
  <read_first>
    .env.example
    .planning/phases/05-docker-packaging-and-local-pilot-deployment/05-CONTEXT.md (D-02, D-12)
    README.md (Deployment section)
  </read_first>
  <files>.env.example, secrets/operator/README.md, secrets/operator/.gitkeep</files>
  <action>
    Update .env.example: add IMAGE=ghcr.io/europeanresolve/er-calendar-bridge:1.0.0 (D-12); document Docker DATA_DIR=/data for compose; keep SYNC_INTERVAL_SECONDS and LOG_LEVEL.

    Move credential placeholders out of committed example where D-02 requires — replace MAILBOX_APP_PASSWORD, GOOGLE_CLIENT_SECRET, GOOGLE_REFRESH_TOKEN with comments pointing to secrets/operator/ files; keep non-secret URLs, usernames, GOOGLE_CALENDAR_ID, NOTIFY optional fields.

    Add secrets/operator/README.md listing exact filenames matching compose secrets and resolve-env mapping; state gitignored operator-created files; include smtp_password only when NOTIFY_OWNER_EMAIL set.

    Add secrets/operator/.gitkeep so directory exists in git without secret content (parent secrets/ still gitignored — use negation pattern if needed: !secrets/operator/.gitkeep and !secrets/operator/README.md per gitignore best practice).

    Adjust .gitignore in this task only if .gitkeep/README cannot be tracked: use secrets/* with exceptions for README and .gitkeep.
  </action>
  <verify>
    <automated>grep -q 'IMAGE=' .env.example && test -f secrets/operator/README.md && grep -q google_refresh_token secrets/operator/README.md</automated>
  </verify>
  <acceptance_criteria>
    - .env.example documents IMAGE and Docker-oriented DATA_DIR
    - README in secrets/operator lists all required secret files for pilot
    - No real credentials in committed files
    - Operator can copy secrets/operator/ template for another member per D-01
  </acceptance_criteria>
  <done>Env example and secret filename docs complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compose.yaml → host filesystem | file: secrets paths read at deploy time |
| .env on host | Non-secrets only; passwords must not appear |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Information Disclosure | Passwords in compose environment | mitigate | secrets: files only; env_file documents non-secrets |
| T-05-09 | Information Disclosure | Published ports expose service | mitigate | No ports (D-11); outbound-only |
| T-05-10 | Spoofing | Shared secrets across members | mitigate | Per-member secrets/&lt;member&gt;/ dirs (SEC-04) |
| T-05-11 | Information Disclosure | .env.example commits secrets | mitigate | Pointers to secret files only (D-02) |
</threat_model>

<verification>
docker compose config; .env.example contains IMAGE; secrets/operator/README.md lists filenames matching 05-01 mapping.
</verification>

<success_criteria>
Compose pilot stack validates; GHCR IMAGE documented; per-file secrets layout documented for operator.
</success_criteria>

<output>
Create `.planning/phases/05-docker-packaging-and-local-pilot-deployment/05-03-SUMMARY.md` when done
</output>
