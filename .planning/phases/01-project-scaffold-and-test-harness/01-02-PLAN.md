---
phase: 01-project-scaffold-and-test-harness
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - test/helpers/load-fixture.ts
  - test/helpers/parse-ics.ts
  - test/helpers/assert-sidecar.ts
  - test/fixtures/public/board-meeting.ics
  - test/fixtures/public/board-meeting.expected.json
  - test/fixtures/internal/team-sync.ics
  - test/fixtures/internal/team-sync.expected.json
  - test/fixtures/sensitive/hr-review.ics
  - test/fixtures/sensitive/hr-review.expected.json
  - test/fixtures/untagged/unclassified.ics
  - test/fixtures/untagged/unclassified.expected.json
  - test/fixtures/recurrence/weekly-series.ics
  - test/fixtures/recurrence/weekly-series.expected.json
  - test/fixtures/recurrence/modified-instance.ics
  - test/fixtures/recurrence/modified-instance.expected.json
  - test/fixtures/recurrence/deleted-instance.ics
  - test/fixtures/recurrence/deleted-instance.expected.json
autonomous: true
requirements:
  - TEST-02
  - TEST-03
user_setup: []

must_haves:
  truths:
    - "Fixture library covers all four classification tiers with raw .ics files"
    - "Each tier fixture has a matching .expected.json sidecar"
    - "Recurrence high-risk set includes series, modified instance, and deleted instance"
    - "Test helpers load fixtures and parse iCal via @pipobscure/ical"
  artifacts:
    - path: test/helpers/load-fixture.ts
      provides: "loadFixture() for tier + name"
      exports: ["loadFixture", "FixtureExpected"]
    - path: test/helpers/parse-ics.ts
      provides: "parseIcs() and getCategories() normalization"
      exports: ["parseIcs", "getCategories"]
    - path: test/fixtures/public/board-meeting.ics
      provides: "ER-PUBLIC tier sample"
      contains: "CATEGORIES:ER-PUBLIC"
    - path: test/fixtures/untagged/unclassified.ics
      provides: "No CATEGORIES property"
      contains: "BEGIN:VEVENT"
    - path: test/fixtures/recurrence/modified-instance.ics
      provides: "RECURRENCE-ID override"
      contains: "RECURRENCE-ID"
  key_links:
    - from: test/helpers/parse-ics.ts
      to: "@pipobscure/ical"
      via: "parse() import"
      pattern: "from '@pipobscure/ical'"
    - from: test/helpers/load-fixture.ts
      to: test/fixtures/
      via: "readFile per tier/name"
      pattern: "FIXTURES_ROOT"
    - from: test/fixtures/public/board-meeting.ics
      to: test/fixtures/public/board-meeting.expected.json
      via: "sidecar pairing"
      pattern: "board-meeting"
---

<objective>
Build the fixture-driven test harness foundation: shared test helpers and the complete iCalendar fixture library with sidecars for all classification tiers and recurrence-exception cases.

Purpose: Provide realistic, inspectable test data and parsing utilities so Phase 2 domain logic and Phase 3 sync can be developed test-first without network calls.
Output: `test/helpers/` utilities; 7 fixture pairs (4 tier + 3 recurrence) per D-05–D-08 and D-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-project-scaffold-and-test-harness/01-CONTEXT.md
@.planning/phases/01-project-scaffold-and-test-harness/01-RESEARCH.md
@.planning/phases/01-project-scaffold-and-test-harness/01-PATTERNS.md
@.planning/phases/01-project-scaffold-and-test-harness/01-01-SUMMARY.md

## Locked decisions implemented in this plan
- D-05: Raw `.ics` fixture files
- D-06: Organize by tier subdirs under `test/fixtures/`
- D-07: Recurrence set — weekly series, modified instance, deleted instance
- D-08: Each fixture paired with `.expected.json` sidecar
- D-15: Shared helpers `loadFixture()`, `parseIcs()`, `assertTier()` scaffold

## Sidecar schema (all fixtures)
Fields: `uid`, `categories` (array), `tier`, `propagation` (`full`|`busy`|`drop`), `washed` (object or null)

## Fixture inventory
| Tier | File | Categories | propagation |
|------|------|------------|-------------|
| public | board-meeting | ER-PUBLIC | full |
| internal | team-sync | ER-INTERNAL | busy |
| sensitive | hr-review | ER-SENSITIVE | drop |
| untagged | unclassified | (omit CATEGORIES line) | busy |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared test helpers</name>
  <read_first>
    .planning/phases/01-project-scaffold-and-test-harness/01-RESEARCH.md (loadFixture and parseIcs code examples)
    .planning/phases/01-project-scaffold-and-test-harness/01-PATTERNS.md (test/helpers sections)
    package.json
    vitest.config.ts
  </read_first>
  <files>test/helpers/load-fixture.ts, test/helpers/parse-ics.ts, test/helpers/assert-sidecar.ts</files>
  <action>
    Create `test/helpers/load-fixture.ts` per D-15/RESEARCH:
    - Export `FixtureExpected` interface with fields `uid`, `categories`, `tier`, `propagation`, `washed`
    - Export async `loadFixture(tier, name)` where tier is `'public' | 'internal' | 'sensitive' | 'untagged' | 'recurrence'`
    - Resolve paths via `import.meta.url` + `fileURLToPath` relative to `test/fixtures/`
    - Read `${name}.ics` and `${name}.expected.json` from tier subdirectory

    Create `test/helpers/parse-ics.ts` per D-15:
    - Import `parse` from `@pipobscure/ical`
    - Export `parseIcs(ics: string)` returning first VEVENT or throw if none
    - Export `getCategories(event)` normalizing `ICalValue[]` to string array (Pitfall 3)

    Create `test/helpers/assert-sidecar.ts` per D-15:
    - Export `assertTier(event, expected)` — Phase 1 scaffold: verify `event.uid === expected.uid` and categories match via `getCategories()`; leave full washed-field comparison as TODO comment for Phase 2
    - Use `.js` extensions in relative imports between helpers

    Do NOT hand-roll an iCal parser — use `@pipobscure/ical` only.
  </action>
  <verify>
    <automated>npm run typecheck 2>/dev/null; node -e "import('./test/helpers/load-fixture.ts').then(m => console.log(typeof m.loadFixture))" 2>&1 | grep -q function || test -f test/helpers/load-fixture.ts</automated>
  </verify>
  <acceptance_criteria>
    - Three helper files exist under `test/helpers/`
    - `loadFixture` accepts tier union including `recurrence`
    - `parseIcs` uses `@pipobscure/ical` `parse()` — no custom VEVENT parser
    - `getCategories` handles non-string category values
    - `assertTier` exported with uid/categories comparison scaffold
    - All relative imports use `.js` extension (NodeNext ESM)
  </acceptance_criteria>
  <done>Test helper module ready to load and parse fixtures; assert-sidecar scaffolded for Phase 2 expansion</done>
</task>

<task type="auto">
  <name>Task 2: Create classification-tier fixture pairs</name>
  <read_first>
    .planning/phases/01-project-scaffold-and-test-harness/01-RESEARCH.md (Pattern 3 sidecar example)
    .planning/phases/01-project-scaffold-and-test-harness/01-PATTERNS.md (fixture inventory table)
    test/helpers/load-fixture.ts
  </read_first>
  <files>
    test/fixtures/public/board-meeting.ics
    test/fixtures/public/board-meeting.expected.json
    test/fixtures/internal/team-sync.ics
    test/fixtures/internal/team-sync.expected.json
    test/fixtures/sensitive/hr-review.ics
    test/fixtures/sensitive/hr-review.expected.json
    test/fixtures/untagged/unclassified.ics
    test/fixtures/untagged/unclassified.expected.json
  </files>
  <action>
    Create four realistic VCALENDAR fixtures per D-05/D-06 with synthetic data only (`@er.example` UIDs, fake names — no real PII per Pitfall 6):

    **public/board-meeting.ics**: Include `CATEGORIES:ER-PUBLIC`, realistic SUMMARY/DTSTART/DTEND with `TZID=Europe/Brussels` or UTC `Z`. Sidecar: `tier: "public"`, `propagation: "full"`, `categories: ["ER-PUBLIC"]`, `washed: null`, uid matching VEVENT.

    **internal/team-sync.ics**: `CATEGORIES:ER-INTERNAL`. Sidecar: `tier: "internal"`, `propagation: "busy"`.

    **sensitive/hr-review.ics**: `CATEGORIES:ER-SENSITIVE`. Sidecar: `tier: "sensitive"`, `propagation: "drop"`.

    **untagged/unclassified.ics**: Omit `CATEGORIES` property entirely (not empty CATEGORIES — per RESEARCH Open Question 2). Include SUMMARY and DTSTART. Sidecar: `tier: "untagged"`, `propagation: "busy"`, `categories: []`.

    Each `.ics` must be valid RFC 5545 VCALENDAR with PRODID, VERSION:2.0, single VEVENT (except recurrence plan handles multi-VEVENT). Pair each with `.expected.json` in same directory per D-08.
  </action>
  <verify>
    <automated>for d in public internal sensitive untagged; do test -f test/fixtures/$d/*.ics && test -f test/fixtures/$d/*.expected.json; done</automated>
  </verify>
  <acceptance_criteria>
    - Four tier subdirectories exist under `test/fixtures/`
    - Each tier has exactly one `.ics` + `.expected.json` pair per PATTERNS inventory
    - `public`, `internal`, `sensitive` fixtures contain correct `ER-*` CATEGORIES
    - `untagged/unclassified.ics` has no CATEGORIES line
    - All UIDs use `@er.example` domain — no real mailbox.org or personal data
    - Sidecar `uid` matches VEVENT UID in corresponding `.ics`
  </acceptance_criteria>
  <done>TEST-02 satisfied: fixture library covers all classification tiers and untagged events with sidecars</done>
</task>

<task type="auto">
  <name>Task 3: Create recurrence-exception fixture pairs</name>
  <read_first>
    .planning/phases/01-project-scaffold-and-test-harness/01-RESEARCH.md (Pattern 4 recurrence fixtures)
    .planning/research/PITFALLS.md
    test/helpers/parse-ics.ts
  </read_first>
  <files>
    test/fixtures/recurrence/weekly-series.ics
    test/fixtures/recurrence/weekly-series.expected.json
    test/fixtures/recurrence/modified-instance.ics
    test/fixtures/recurrence/modified-instance.expected.json
    test/fixtures/recurrence/deleted-instance.ics
    test/fixtures/recurrence/deleted-instance.expected.json
  </files>
  <action>
    Create three high-risk recurrence fixtures per D-07 in `test/fixtures/recurrence/`:

    **weekly-series.ics**: Single VEVENT with `RRULE:FREQ=WEEKLY` (add COUNT or UNTIL), `CATEGORIES:ER-INTERNAL`. Sidecar notes `tier: "internal"`, documents expected `rrules` presence.

    **modified-instance.ics**: Multi-VEVENT file per RESEARCH example — master VEVENT with RRULE + second VEVENT with same UID and `RECURRENCE-ID` matching master instance time. Use consistent `TZID=Europe/Brussels` across master and RECURRENCE-ID (Pitfall 4). Sidecar uid matches series UID.

    **deleted-instance.ics**: Master with RRULE plus either `EXDATE` listing removed occurrence OR exception VEVENT with `STATUS:CANCELLED`. Consistent TZID. Sidecar documents deletion mechanism used.

    Each file gets `.expected.json` sidecar with `uid`, `categories`, `tier`, `propagation`, `washed` — even though Phase 1 smoke only checks parseability, sidecars must exist per D-08.

    Manually verify each file parses via `parse()` without error before committing (quick node/tsx one-liner acceptable).
  </action>
  <verify>
    <automated>for f in weekly-series modified-instance deleted-instance; do test -f test/fixtures/recurrence/$f.ics && test -f test/fixtures/recurrence/$f.expected.json; done && grep -q RRULE test/fixtures/recurrence/weekly-series.ics && grep -q RECURRENCE-ID test/fixtures/recurrence/modified-instance.ics && (grep -q EXDATE test/fixtures/recurrence/deleted-instance.ics || grep -q CANCELLED test/fixtures/recurrence/deleted-instance.ics)</automated>
  </verify>
  <acceptance_criteria>
    - Three recurrence fixture pairs exist under `test/fixtures/recurrence/`
    - `weekly-series.ics` contains RRULE:FREQ=WEEKLY
    - `modified-instance.ics` contains RECURRENCE-ID with TZID consistent with master DTSTART
    - `deleted-instance.ics` uses EXDATE or STATUS:CANCELLED for deleted occurrence
    - All three parse successfully with `@pipobscure/ical` without throw
    - Synthetic data only — no real PII
  </acceptance_criteria>
  <done>TEST-03 satisfied: recurrence-exception fixtures exist as explicit high-risk test targets</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture files → git | Test data committed to repo; must not contain secrets or real PII |
| @pipobscure/ical parse | Untrusted iCal input in tests only — no runtime user input in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | test/fixtures/*.ics | mitigate | Synthetic UIDs (`@er.example`), fake names; no real mailbox credentials or attendee emails |
| T-01-05 | Information Disclosure | sidecar washed fields | accept | Sidecars document expected washed output for Phase 2; no live data |
| T-01-06 | Tampering | fixture content | mitigate | Sidecar uid must match VEVENT UID; smoke test in plan 01-03 validates pairing |
</threat_model>

<verification>
After plan 01-02:
- All 7 fixture pairs exist (4 tier + 3 recurrence)
- Helpers can load any fixture: `loadFixture('public', 'board-meeting')` returns ics + expected
- Recurrence files contain RRULE, RECURRENCE-ID, and EXDATE/CANCELLED respectively
</verification>

<success_criteria>
1. `test/helpers/` provides loadFixture, parseIcs, getCategories, assertTier scaffold
2. All four classification tiers represented with `.ics` + `.expected.json` (TEST-02)
3. Three recurrence-exception fixtures cover series, override, and deletion (TEST-03)
4. No real PII or credentials in fixture files
</success_criteria>

<output>
Create `.planning/phases/01-project-scaffold-and-test-harness/01-02-SUMMARY.md` when done
</output>
