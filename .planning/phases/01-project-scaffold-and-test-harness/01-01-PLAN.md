---
phase: 01-project-scaffold-and-test-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vitest.config.ts
  - eslint.config.mjs
  - .gitignore
  - .env.example
  - src/index.ts
  - src/domain/types/index.ts
  - src/domain/.gitkeep
  - src/adapters/.gitkeep
  - src/sync/.gitkeep
autonomous: false
requirements:
  - TEST-01
  - OPS-04
user_setup: []

must_haves:
  truths:
    - "npm install succeeds with locked Node 24 ESM toolchain"
    - "npm run typecheck, lint, and build succeed on empty scaffold"
    - "src/ uses three-layer layout with domain types stub and entry point"
    - ".env.example documents future config keys without secrets"
  artifacts:
    - path: package.json
      provides: "ESM project manifest with test/lint/typecheck/build scripts"
      contains: '"type": "module"'
    - path: tsconfig.json
      provides: "Strict TypeScript compile config"
      contains: '"strict": true'
    - path: vitest.config.ts
      provides: "Node test runner config"
      contains: "environment: 'node'"
    - path: src/index.ts
      provides: "Minimal entry stub for tsc and tsx"
      exports: ["VERSION", "main"]
    - path: src/domain/types/index.ts
      provides: "Shared domain type stubs"
      exports: ["Tier", "SourceEvent", "PropagationDecision"]
  key_links:
    - from: package.json
      to: vitest.config.ts
      via: "npm test script"
      pattern: '"test": "vitest run"'
    - from: src/index.ts
      to: src/domain/types/index.ts
      via: "optional type import readiness"
      pattern: "domain/types"
---

<objective>
Bootstrap the strict TypeScript ESM project skeleton: package manifest, tooling configs, three-layer `src/` layout, and config documentation — with human-verified npm dependencies before install.

Purpose: Establish the compile/lint/test toolchain and source boundaries so fixture harness and domain logic can land test-first in later plans.
Output: Runnable `npm run typecheck`, `npm run lint`, `npm run build`; committed `package-lock.json`; scaffolded `src/` per D-01–D-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-scaffold-and-test-harness/01-CONTEXT.md
@.planning/phases/01-project-scaffold-and-test-harness/01-RESEARCH.md
@.planning/phases/01-project-scaffold-and-test-harness/01-PATTERNS.md

## Locked decisions implemented in this plan
- D-01: `src/domain/`, `src/adapters/`, `src/sync/` with `.gitkeep` only
- D-02: `src/domain/types/` holds `Tier`, `SourceEvent`, `PropagationDecision`
- D-03: `src/index.ts` minimal stub (no CLI)
- D-04: `.gitkeep` only in empty folders — no README per folder
- D-09: `engines.node: "24.x"`
- D-10: npm + committed `package-lock.json`
- D-11: scripts `test`, `lint`, `typecheck`, `build`
- D-12: eslint only — no Prettier

## Package versions (verify at install time)
- Runtime: `@pipobscure/ical@1.0.0`
- Dev: `typescript@5.9.3`, `vitest@5.0.0`, `tsx@4.23.13`, `eslint@10.10.0`, `@eslint/js@10.0.1`, `typescript-eslint@8.70.0`, `@types/node@26.5.1`
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify npm package legitimacy before install</name>
  <files>package.json</files>
  <read_first>
    .planning/phases/01-project-scaffold-and-test-harness/01-RESEARCH.md (Package Legitimacy Audit section)
    .planning/research/STACK.md
  </read_first>
  <action>
    Pause execution before any `npm install`. Present the Package Legitimacy Audit table from RESEARCH to the human reviewer. Request explicit approval for all 8 packages, with special callout for @pipobscure/ical (low downloads, maintainer pipobscure, repo github.com/pipobscure/ical). Do not proceed to Task 2 until human types "approved" or lists exclusions. No files are created in this task — it is a supply-chain gate only.
  </action>
  <what-built>Nothing installed yet — this gate precedes any `npm install`.</what-built>
  <how-to-verify>
    1. Open each package on npmjs.com and confirm publisher + source repo match expectations:
       - typescript → github.com/microsoft/TypeScript
       - vitest → github.com/vitest-dev/vitest
       - eslint → github.com/eslint/eslint
       - typescript-eslint → github.com/typescript-eslint/typescript-eslint
       - tsx → github.com/privatenumber/tsx
       - @types/node → DefinitelyTyped
       - @eslint/js → eslint/eslint
    2. **Special attention — @pipobscure/ical** (low downloads, deliberate stack choice):
       - Confirm package author is `pipobscure`
       - Confirm source repo is github.com/pipobscure/ical
       - Confirm version 1.0.0 exists and is pure ESM
    3. Reject any package that looks typosquatted or has no authoritative source repo.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all 8 packages are legitimate and match STACK.md choices
    - Human explicitly approves @pipobscure/ical despite low download count
    - No `[SLOP]` packages proceed to install
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-project-scaffold-and-test-harness/01-RESEARCH.md</automated>
  </verify>
  <done>Human approval recorded; executor may proceed to npm install in Task 2</done>
  <resume-signal>Type "approved" after verifying packages, or list packages to exclude</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create tooling configs and install dependencies</name>
  <read_first>
    .planning/phases/01-project-scaffold-and-test-harness/01-RESEARCH.md (Patterns 1, 2, 5)
    .planning/phases/01-project-scaffold-and-test-harness/01-PATTERNS.md (package.json, tsconfig, vitest, eslint sections)
  </read_first>
  <files>package.json, package-lock.json, tsconfig.json, vitest.config.ts, eslint.config.mjs, .gitignore</files>
  <action>
    Create `package.json` with: `"name": "er-calendar-bridge"`, `"version": "0.0.0"`, `"type": "module"`, `"engines": { "node": "24.x" }`, `"private": true`. Scripts per D-11: `"test": "vitest run"`, `"test:watch": "vitest"`, `"lint": "eslint ."`, `"typecheck": "tsc --noEmit"`, `"build": "tsc -p tsconfig.json"`. No Prettier per D-12/OPS-04.

    Create `tsconfig.json` per RESEARCH Pattern 1: `target ES2022`, `module`/`moduleResolution` `NodeNext`, `rootDir src`, `outDir dist`, `strict true`, `noUncheckedIndexedAccess true`, `include ["src/**/*.ts"]`, `exclude ["node_modules", "dist", "test"]`.

    **Accepted MVP — tsconfig excludes `test/` (RESEARCH Pitfall 2):** `npm run typecheck` covers `src/` only; vitest compiles test files at runtime via its own pipeline. A dedicated `tsconfig.test.json` extending the base config is deferred to Phase 01.1 CI if typed linting or test-file typecheck in CI is needed. Do not add `tsconfig.test.json` in Phase 1.

    Create `vitest.config.ts` per Pattern 2: `environment: 'node'`, `include: ['test/**/*.test.ts']`, `globals: false`.

    Create `eslint.config.mjs` per Pattern 5: flat config with `@eslint/js` recommended + `typescript-eslint` `recommended` only (not `strictTypeChecked`). Ignore `dist/`, `node_modules/`, `.vitest/`.

    Create `.gitignore` with: `node_modules/`, `dist/`, `.env`, `.vitest/`.

    Run `npm install @pipobscure/ical@1.0.0` and `npm install -D typescript@5.9.3 vitest@5.0.0 tsx@4.23.13 eslint@10.10.0 @eslint/js@10.0.1 typescript-eslint@8.70.0 @types/node@26.5.1`. Commit resulting `package-lock.json` per D-10.

    Do NOT install CalDAV/API/SQLite deps — Phase 1 scope only per OPS-04.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` has `"type": "module"` and `engines.node` set to `24.x`
    - All four scripts exist and execute without error on empty scaffold
    - `package-lock.json` is generated and lists exact versions from RESEARCH
    - `dist/index.js` exists after build
    - No Prettier or formatter dependency added
  </acceptance_criteria>
  <done>Tooling configs committed; typecheck, lint, and build pass with zero application logic beyond empty compile graph</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold source layout, domain types, and env template</name>
  <read_first>
    .planning/phases/01-project-scaffold-and-test-harness/01-CONTEXT.md (D-01 through D-04)
    .planning/phases/01-project-scaffold-and-test-harness/01-RESEARCH.md (src/index.ts and .env.example examples)
    src/index.ts
    tsconfig.json
  </read_first>
  <files>src/index.ts, src/domain/types/index.ts, src/domain/.gitkeep, src/adapters/.gitkeep, src/sync/.gitkeep, .env.example</files>
  <action>
    Create three-layer layout per D-01: `src/domain/`, `src/adapters/`, `src/sync/` each with `.gitkeep` only (D-04). Add `src/domain/types/index.ts` per D-02 exporting:
    - `type Tier = 'public' | 'internal' | 'sensitive' | 'untagged'`
    - `type PropagationDecision = 'full' | 'busy' | 'drop'`
    - `interface SourceEvent { uid: string; categories: string[] }` (stub — expanded Phase 2)

    Create `src/index.ts` per D-03: export `VERSION = '0.0.0'`, export `main()` that logs scaffold message, guard with `import.meta.url` check for direct `tsx src/index.ts` execution. Use `.js` extension if importing from domain types (NodeNext convention).

    Create `.env.example` with placeholder keys from RESEARCH (MAILBOX_CALDAV_URL, MAILBOX_USERNAME, MAILBOX_APP_PASSWORD, GOOGLE_*, MICROSOFT_*, SMTP_*, LOG_LEVEL, SYNC_INTERVAL_MINUTES). All values synthetic/empty — no real credentials. Add comment that mailbox.org access is read-only (enforced Phase 3).

    Re-run typecheck and build to confirm `src/` compiles to `dist/`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && test -f dist/index.js && test -f src/domain/types/index.ts && test -f .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/`, `src/adapters/`, `src/sync/` exist with `.gitkeep` only — no subfolder READMEs
    - `src/domain/types/index.ts` exports Tier, SourceEvent, PropagationDecision
    - `src/index.ts` exports VERSION and main; `node dist/index.js` or `npx tsx src/index.ts` prints scaffold message
    - `.env.example` has no secret values; `.env` is gitignored
    - Domain layer has zero imports from adapters or sync
  </acceptance_criteria>
  <done>Three-layer source scaffold compiles; domain type stubs and config template ready for test harness in plan 01-02</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine | Unverified packages could execute malicious postinstall scripts |
| `.env.example` → git | Placeholder file must not contain real credentials |
| `src/` → future adapters | Domain types must not import adapter code (boundary integrity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm package install | mitigate | Human-verify all `[ASSUMED]` packages before install; reject `[SLOP]`; pin exact versions in package-lock.json |
| T-01-02 | Information Disclosure | .env.example | mitigate | Placeholder values only (`your-app-password-here`, empty OAuth fields); `.gitignore` excludes `.env` |
| T-01-03 | Tampering | eslint/tsconfig | accept | Dev-time tooling only; no runtime exposure; strict tsc enforces type safety |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck unavailable — blocking human checkpoint before Task 2 install; special review for @pipobscure/ical |
</threat_model>

<verification>
After plan 01-01:
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm run build` produces `dist/`
- `npm test` may fail (no tests yet) — expected until plan 01-04
</verification>

<success_criteria>
1. All npm dependencies human-verified and installed with committed lockfile
2. Strict TypeScript ESM project compiles with three-layer `src/` layout
3. Four npm scripts operational; no Prettier or disproportionate tooling (OPS-04)
4. `.env.example` documents future keys without secrets
</success_criteria>

<output>
Create `.planning/phases/01-project-scaffold-and-test-harness/01-01-SUMMARY.md` when done
</output>
