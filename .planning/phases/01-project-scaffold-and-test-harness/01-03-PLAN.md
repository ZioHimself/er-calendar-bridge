---
phase: 01-project-scaffold-and-test-harness
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - test/smoke.test.ts
autonomous: true
requirements:
  - TEST-01
user_setup: []

must_haves:
  truths:
    - "npm test runs vitest and passes at least one meaningful smoke test suite"
    - "Smoke test parses one fixture per classification tier"
    - "Smoke test parses all three recurrence-exception fixtures"
    - "All four npm scripts pass as phase gate"
  artifacts:
    - path: test/smoke.test.ts
      provides: "D-16 fixture-driven smoke test"
      contains: "fixture harness smoke"
  key_links:
    - from: test/smoke.test.ts
      to: test/helpers/load-fixture.ts
      via: "loadFixture import"
      pattern: "loadFixture"
    - from: test/smoke.test.ts
      to: test/fixtures/
      via: "tier fixture iteration"
      pattern: "board-meeting|team-sync|hr-review|unclassified"
    - from: package.json
      to: test/smoke.test.ts
      via: "npm test → vitest run"
      pattern: '"test": "vitest run"'
---

<objective>
Wire the fixture harness into a passing vitest smoke suite and verify the full Phase 1 toolchain gate: test, typecheck, lint, and build all green.

Purpose: Prove the test-first foundation works end-to-end — not a trivial hello-world, but real iCal parsing against tier and recurrence fixtures per D-16.
Output: `test/smoke.test.ts` with passing tests; all four npm scripts exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-project-scaffold-and-test-harness/01-CONTEXT.md
@.planning/phases/01-project-scaffold-and-test-harness/01-RESEARCH.md
@.planning/phases/01-project-scaffold-and-test-harness/01-PATTERNS.md
@.planning/phases/01-project-scaffold-and-test-harness/01-02-SUMMARY.md

## Interfaces from prior plans

From test/helpers/load-fixture.ts:
- `loadFixture(tier, name)` → `{ ics: string; expected: FixtureExpected }`
- `FixtureExpected`: `{ uid, categories, tier, propagation, washed }`

From test/helpers/parse-ics.ts:
- `parseIcs(ics: string)` → first VEVENT Event
- `getCategories(event)` → string[]

## D-16 smoke test requirements
- Load one `.ics` per tier: public, internal, sensitive, untagged
- Parse successfully; assert UID matches sidecar
- For tagged tiers: assert CATEGORIES present and match sidecar
- For untagged: assert UID present and categories array is empty (CATEGORIES property omitted — do NOT require categories.length > 0)
- Parse all three recurrence fixtures without error; assert uid truthy

## Tier fixture names (from 01-02)
- public/board-meeting
- internal/team-sync
- sensitive/hr-review
- untagged/unclassified
- recurrence: weekly-series, modified-instance, deleted-instance
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fixture harness smoke test</name>
  <read_first>
    .planning/phases/01-project-scaffold-and-test-harness/01-RESEARCH.md (smoke test example, D-16)
    .planning/phases/01-project-scaffold-and-test-harness/01-CONTEXT.md (D-16, D-13)
    test/helpers/load-fixture.ts
    test/helpers/parse-ics.ts
    test/helpers/assert-sidecar.ts
    vitest.config.ts
  </read_first>
  <files>test/smoke.test.ts</files>
  <behavior>
    - Tagged tier fixtures (public, internal, sensitive): parse succeeds, event.uid equals sidecar uid, getCategories contains every sidecar category
    - Untagged fixture: parse succeeds, event.uid equals sidecar uid, getCategories returns empty array
    - Recurrence fixtures (weekly-series, modified-instance, deleted-instance): parse succeeds, event.uid is truthy
    - Test suite is NOT a hello-world string assertion — must call loadFixture and parseIcs
  </behavior>
  <action>
    Create `test/smoke.test.ts` per D-16/RESEARCH smoke test pattern:

    Describe block: `'fixture harness smoke'`.

    Parameterized test over `TIER_FIXTURES` array:
    - `{ tier: 'public', name: 'board-meeting' }`
    - `{ tier: 'internal', name: 'team-sync' }`
    - `{ tier: 'sensitive', name: 'hr-review' }`
    - `{ tier: 'untagged', name: 'unclassified' }`

    For each: `loadFixture(tier, name)`, `parseIcs(ics)`, assert `event.uid === expected.uid`.

    Category assertions (branch on tier):
    - If tier is `untagged`: `expect(getCategories(event)).toEqual([])` or length 0
    - Else: `expect(getCategories(event).length).toBeGreaterThan(0)` and each `expected.categories` entry is contained in result

    Separate `it` block for recurrence: loop `['weekly-series', 'modified-instance', 'deleted-instance']`, load from `'recurrence'` tier, parse, assert `event.uid` truthy.

    Import helpers with `.js` extensions. Import `describe`, `it`, `expect` from `vitest` (globals: false).

    Optional: call `assertTier(event, expected)` from assert-sidecar helper where applicable.

    Do NOT add network calls, mocks, or domain classify/wash logic — unit-first per D-13.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `test/smoke.test.ts` exists and is picked up by vitest (`test/**/*.test.ts`)
    - `npm test` exits 0 with all smoke tests passing
    - At least 5 test cases run (4 tier + 1 recurrence group)
    - Untagged test asserts empty categories — not `toBeGreaterThan(0)`
    - No trivial `expect(true).toBe(true)` assertions
  </acceptance_criteria>
  <done>TEST-01 smoke portion complete: vitest runs strict TypeScript tests against real iCal fixtures</done>
</task>

<task type="auto">
  <name>Task 2: Run full Phase 1 validation gate</name>
  <read_first>
    .planning/ROADMAP.md (Phase 1 success criteria)
    .planning/phases/01-project-scaffold-and-test-harness/01-RESEARCH.md (Validation Architecture sampling rate)
    package.json
    test/smoke.test.ts
  </read_first>
  <files>package.json</files>
  <action>
    Run the full Phase 1 gate sequence in order:
    1. `npm test` — vitest smoke suite
    2. `npm run typecheck` — strict tsc on src/
    3. `npm run lint` — eslint flat config
    4. `npm run build` — tsc emit to dist/

    If any command fails, fix the root cause in the failing layer (do not disable strict mode or skip tests).

    Verify ROADMAP Phase 1 success criteria structurally:
    - Fixture dirs: public, internal, sensitive, untagged, recurrence all populated
    - `src/domain`, `src/adapters`, `src/sync` exist
    - `.env.example` exists

    No code changes required if all pass — this task is the integration gate. Only modify files if fixes needed.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All four npm scripts exit 0 in sequence
    - `dist/index.js` exists after build
    - ROADMAP success criteria 1–5 structurally met (test, typecheck, lint, fixtures, src layout, .env.example)
    - No CalDAV/API dependencies added beyond Phase 1 scope
  </acceptance_criteria>
  <done>Phase 1 goal achieved: strict TypeScript project with vitest harness and complete fixture library, all tooling green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vitest → filesystem | Tests read fixture files from disk — path traversal not applicable (fixed test paths) |
| CI readiness (Phase 01.1) | `npm test` must exit cleanly for future CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Elevation | disabled strict checks | mitigate | Gate requires `tsc --noEmit` with strict:true; no `--skipLibCheck` removal or strict bypass |
| T-01-08 | Information Disclosure | test output | accept | Smoke test logs only synthetic fixture UIDs |
| T-01-09 | Denial of Service | vitest hang | accept | Local dev only; no watch mode in CI script (`vitest run` exits) |
</threat_model>

<verification>
Phase 1 complete when:
```bash
npm test && npm run typecheck && npm run lint && npm run build
```
All exit 0. Fixture count: 4 tier + 3 recurrence pairs minimum.
</verification>

<success_criteria>
1. `npm test` passes with fixture-driven smoke tests (not hello-world)
2. `npm run typecheck`, `npm run lint`, `npm run build` all succeed
3. D-16 satisfied: UID assertions on all tiers; category assertions on tagged tiers only
4. Recurrence fixtures parse in smoke suite
5. Phase ready for `/gsd:execute-phase 01.1` (CI) without structural changes
</success_criteria>

<output>
Create `.planning/phases/01-project-scaffold-and-test-harness/01-03-SUMMARY.md` when done
</output>
