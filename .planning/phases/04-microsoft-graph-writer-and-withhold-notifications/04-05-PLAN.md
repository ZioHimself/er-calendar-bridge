---
phase: 04-microsoft-graph-writer-and-withhold-notifications
plan: 05
type: tdd
wave: 3
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - src/sync/types.ts
  - src/sync/run-sync-cycle.ts
  - src/index.ts
  - test/integration/sync-cycle.test.ts
autonomous: true
requirements:
  - SYNC-14
  - SYNC-15
  - SYNC-16
  - OPS-03
user_setup: []

must_haves:
  truths:
    - "Busy and drop propagation transitions trigger withhold handler; full does not mail (D-01, D-02)"
    - "CalDAV tombstone deletes do not call WithholdNotifier (D-04)"
    - "First-sight drop persists bridge_uuid in notify state before early return (D-13 discretion)"
    - "Second poll same busy state does not call notifier again (D-14, SYNC-15)"
    - "full → busy → full → busy sends notifier twice across episodes (D-03)"
    - "Audit row appended on withhold transition with notify_status sent/failed/disabled per outcome (D-16, D-08)"
    - "SMTP failure does not increment sync cycle errors or abort cycle (D-08, Phase 3 D-18)"
    - "Sync cycle still records last_success_at and counts when notify fails (OPS-03)"
  artifacts:
    - path: src/sync/run-sync-cycle.ts
      provides: handleWithholdSideEffects wired in handleOutboundEvent
    - path: src/sync/types.ts
      provides: SyncCycleDeps with auditStore and withholdNotifier
    - path: test/integration/sync-cycle.test.ts
      provides: withhold mock assertions
  key_links:
    - from: src/sync/run-sync-cycle.ts
      to: src/sync/withhold-transition.ts
      via: computeWithholdTransition
      pattern: computeWithholdTransition
    - from: src/sync/run-sync-cycle.ts
      to: src/store/audit-store.ts
      via: getNotifyState upsert append
      pattern: appendAuditRow
    - from: src/index.ts
      to: src/notify/withhold-notifier.ts
      via: createWithholdNotifier in sync command
      pattern: createWithholdNotifier
---

<objective>
Wire withhold transition, audit persistence, and injectable notifier into the Google sync loop; extend integration tests for SYNC-14–16 and all notify trigger decisions.

Purpose: Deliver end-to-end withhold behavior in CI without live SMTP; extend OPS-03 with per-event audit trail population during sync.
Output: Updated `run-sync-cycle.ts`, `SyncCycleDeps`, integration tests, production notifier wiring in `index.ts` sync path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-CONTEXT.md (D-01 through D-08, D-16)
@.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-RESEARCH.md (Pattern 4, Pitfall 1)
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-06-SUMMARY.md
@test/integration/sync-cycle.test.ts
@src/sync/run-sync-cycle.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Integration tests for withhold notify and audit</name>
  <read_first>
    test/integration/sync-cycle.test.ts
    test/helpers/load-fixture.ts
    .planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-VALIDATION.md
  </read_first>
  <files>test/integration/sync-cycle.test.ts</files>
  <behavior>
    - internal tier first cycle: withholdNotifier.notifyWithhold called once; second cycle same fixture: not called (D-02, D-14)
    - sensitive drop first sight without mapping: notifier called; auditStore has row with dedup_key containing drop (D-01, Pitfall 1)
    - public then internal across cycles: notifier called on internal transition; not on repeated internal (D-01 busy)
    - public → internal → public → internal: notifier called twice (D-03 episode)
    - deleted delta test: withholdNotifier not called (D-04)
    - notifyOwnerEmail disabled path: audit append with notify_status disabled when notifier no-op (D-05, D-16)
    - mock notifier rejects: cycle completes errors count unchanged from Google-only baseline; audit notify_status failed (D-08)
  </behavior>
  <action>
    Extend buildDeps in test/integration/sync-cycle.test.ts to open openAuditStore(':memory:') on same DB path as mapping store (use shared :memory: path or single sqlite file in temp — prefer one openAuditStore + openMappingStore on same ':memory:' only if schema shared; use file path ':memory:' per store may differ — use explicit shared sqlitePath ':memory:' for all stores on same connection pattern: mapping-store uses path string; for tests use a temp file or single Database — follow audit-store test pattern: if separate :memory: per module fails, use tmp file).

    Add withholdNotifier: vi.fn().mockResolvedValue({ status: 'sent' }) to deps.

    Add tests before implementation; expect failures until Task 2.

    Commit RED.
  </action>
  <verify>
    <automated>npm test -- test/integration/sync-cycle.test.ts -t withhold; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - New tests named or tagged withhold for targeted runs
    - D-04 tombstone case explicitly asserts notifier not called
    - Mocks only — no live SMTP (TEST-05)
  </acceptance_criteria>
  <done>RED: sync withhold integration tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): handleWithholdSideEffects in sync loop</name>
  <read_first>
    test/integration/sync-cycle.test.ts
    src/sync/withhold-transition.ts
    src/store/audit-store.ts
    .planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-RESEARCH.md
  </read_first>
  <files>src/sync/types.ts, src/sync/run-sync-cycle.ts</files>
  <action>
    Extend SyncCycleDeps with auditStore: AuditStore and withholdNotifier: WithholdNotifier (required in production; tests inject mock).

    Add internal async function handleWithholdSideEffects(deps, source, processed, bridgeUuid) called from handleOutboundEvent:

    Load notify state by uid/recurrenceId. Resolve bridgeUuid: mapping?.bridgeUuid ?? state?.bridge_uuid ?? new randomUUID() — persist before drop early return.

    Call computeWithholdTransition with previousPropagation from state?.last_propagation, current from processed.propagation, episode from state?.episode ?? 0.

    If !shouldRecord: if current is full, still upsert state last_propagation full without audit; return.

    After Google writer cancel/upsert completes for this event, if shouldRecord: determine notify_status — disabled when deps config indicates no notify email (pass notifyEnabled boolean on deps or check notifier no-op); else call withholdNotifier; map result to sent/failed; on catch log error and failed (D-08) without rethrow.

    appendAuditRow with tier, propagation, dedup_key, notify_status, error message if failed (D-16). upsertNotifyState with nextEpisode and nextPropagation.

    Insert call sites: on drop branch after cancel/markCancelled (or before return when no mapping) still run withhold with bridge uuid; on busy upsert path after mapping upsert; when propagation full after upsert, update state to full without notify.

    handleDeletedTombstone: do not call withhold handler (D-04).

    Do not increment result.errors on SMTP-only failures (D-08).

    Run integration withhold tests until green.
  </action>
  <verify>
    <automated>npm test -- test/integration/sync-cycle.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All integration tests pass including pre-existing Phase 3 cases
    - handleOutboundEvent drop-first-sight persists bridge uuid in notify state
    - Dedup prevents duplicate notify on unchanged busy (SYNC-15)
  </acceptance_criteria>
  <done>GREEN: sync loop withhold wiring</done>
</task>

<task type="auto">
  <name>Task 3: Wire audit store and notifier in CLI sync path</name>
  <read_first>
    src/index.ts
    src/notify/withhold-notifier.ts
    src/store/audit-store.ts
  </read_first>
  <files>src/index.ts</files>
  <action>
    In sync command path: openAuditStore(config.sqlitePath) alongside mapping and sync state stores; close in finally.

    Pass createWithholdNotifier(config) as withholdNotifier and auditStore into runSyncCycle deps.

    Add pino redact paths for SMTP_PASSWORD and smtpPassword camelCase field if logged (D-08).

    Run full test suite and lint.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Production sync command provides auditStore and withholdNotifier to runSyncCycle
    - All three stores closed on exit
    - No Microsoft Graph or Docker changes in this task
  </acceptance_criteria>
  <done>CLI sync path wires withhold dependencies</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sync loop → owner SMTP | Withhold metadata crosses to mail relay on transition only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information disclosure | audit on sync | mitigate | D-16 fields only; no summary in appendAuditRow |
| T-04-11 | Denial of service | SMTP failure | mitigate | D-08 inner catch; cycle continues |
| T-04-12 | Repudiation | withhold events | mitigate | Append-only audit on each transition (SYNC-16) |
| T-04-13 | Elevation | erroneous notify | mitigate | D-02 on-change; computeWithholdTransition unit tested in 04-01 |
</threat_model>

<verification>
npm test && npm run typecheck && npm run lint
</verification>

<success_criteria>
Mocked integration proves SYNC-14–16 behaviors; production sync opens audit store and notifier.
</success_criteria>

<output>
Create `.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-05-SUMMARY.md` when done
</output>
