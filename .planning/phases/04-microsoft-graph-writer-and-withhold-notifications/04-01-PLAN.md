---
phase: 04-microsoft-graph-writer-and-withhold-notifications
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/sync/withhold-transition.ts
  - test/sync/withhold-transition.test.ts
autonomous: false
requirements:
  - SYNC-15
user_setup: []

must_haves:
  truths:
    - "Busy and drop transitions notify once; unchanged busy/drop across polls does not (D-02, D-14, SYNC-15)"
    - "Full disclosure between withhold episodes increments episode and allows a new dedup key (D-03, D-13)"
    - "Dedup key format is bridgeUuid:propagation:episode (D-13)"
    - "Tier-only changes with same propagation do not trigger transition (D-02)"
    - "Full propagation never records withhold transition or dedup key"
  artifacts:
    - path: src/sync/withhold-transition.ts
      provides: computeWithholdTransition pure logic
      exports: ["computeWithholdTransition", "WithholdPropagation"]
    - path: test/sync/withhold-transition.test.ts
      provides: episode and suppression unit tests
  key_links:
    - from: test/sync/withhold-transition.test.ts
      to: src/sync/withhold-transition.ts
      via: import computeWithholdTransition
      pattern: computeWithholdTransition
---

<objective>
Establish nodemailer dependency (with legitimacy gate) and implement pure withhold transition logic — episode, dedup key, and on-change notify decisions — test-first.

Purpose: SYNC-15 and locked D-02/D-03/D-13/D-14 must be correct before any SQLite or SMTP I/O.
Output: `withhold-transition.ts` with full vitest coverage; `nodemailer` in package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-CONTEXT.md (D-02, D-03, D-13, D-14)
@.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-RESEARCH.md (Pattern 1, Package Legitimacy Audit)
@.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-PATTERNS.md (withhold-transition section)
@src/domain/process-source-event.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify nodemailer package legitimacy</name>
  <read_first>
    .planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-RESEARCH.md (Package Legitimacy Audit)
    .planning/research/STACK.md
  </read_first>
  <files>package.json</files>
  <action>
    Pre-install legitimacy gate for nodemailer marked [ASSUMED] in research — no npm install until human approves.

    Present verification steps to operator:
    1. Open https://www.npmjs.com/package/nodemailer and confirm publisher, weekly downloads, and current major version.
    2. Open https://github.com/nodemailer/nodemailer — verify source repo matches npm metadata.
    3. Confirm no typosquat (package name exactly nodemailer, not nodemailer-core variants).

    Pause until operator types approved; then Task 2 may run npm install nodemailer.
  </action>
  <verify>
    <automated>test -f package.json</automated>
  </verify>
  <acceptance_criteria>
    - Human explicitly approved nodemailer install
    - No nodemailer dependency added before approval
  </acceptance_criteria>
  <done>Legitimacy checkpoint cleared for nodemailer</done>
  <what-built>Pre-install legitimacy gate for nodemailer marked [ASSUMED] in research</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/nodemailer and confirm publisher, weekly downloads, and current major version.
    2. Open https://github.com/nodemailer/nodemailer — verify source repo matches npm metadata.
    3. Confirm no typosquat (package name exactly `nodemailer`, not nodemailer-core variants).
    4. Type "approved" to allow `npm install nodemailer` in Task 2.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (RED): Failing withhold-transition tests</name>
  <read_first>
    .planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-RESEARCH.md (computeWithholdTransition shape)
    test/domain/classify.test.ts
    src/domain/process-source-event.ts
  </read_first>
  <files>test/sync/withhold-transition.test.ts</files>
  <behavior>
    - undefined → busy: shouldRecord true, shouldAttemptNotify true, episode 1, dedupKey `uuid:busy:1` (D-01 busy path)
    - undefined → drop: same with drop propagation (D-01 drop path)
    - busy → busy: shouldRecord false, shouldAttemptNotify false, dedupKey null (D-02, D-14)
    - busy → drop: new transition, dedupKey includes drop and same or bumped episode per RESEARCH busy→drop note
    - full → busy: episode increments from prior episode; dedupKey changes (D-03)
    - full → busy → full → busy: second busy transition yields higher episode and distinct dedup keys (D-03)
    - full propagation: shouldRecord false, nextPropagation full, dedupKey null
    - tier change only (caller passes same current/previous propagation busy): no transition — document via two busy→busy cases
  </behavior>
  <action>
    After checkpoint approval, run `npm view nodemailer version` and `npm install nodemailer` (pin exact version returned by npm view in package.json).

    Create test/sync/withhold-transition.test.ts importing computeWithholdTransition from src/sync/withhold-transition.js — module missing until Task 3.

    Use fixed bridgeUuid string in assertions for dedupKey equality.

    Commit RED; verify tests fail because module or function missing.
  </action>
  <verify>
    <automated>npm test -- test/sync/withhold-transition.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - nodemailer listed in package.json dependencies after install
    - Test file covers D-02, D-03, D-13, D-14 scenarios without network or SQLite
    - RED commit: tests fail for missing implementation
  </acceptance_criteria>
  <done>RED: withhold-transition test suite committed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN): Implement computeWithholdTransition</name>
  <read_first>
    test/sync/withhold-transition.test.ts
    .planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-RESEARCH.md (Pattern 1)
  </read_first>
  <files>src/sync/withhold-transition.ts</files>
  <action>
    Create src/sync/withhold-transition.ts exporting type WithholdPropagation = 'full' | 'busy' | 'drop' and computeWithholdTransition per RESEARCH Pattern 1.

    Return shape: shouldRecord, shouldAttemptNotify, nextEpisode, nextPropagation, dedupKey (string | null).

    Episode rules per D-03: increment when previousPropagation was 'full'; first withhold from undefined starts episode at 1.

    Do not import nodemailer, SQLite, or domain washing — pure logic only.

    Run tests until green; run typecheck.
  </action>
  <verify>
    <automated>npm test -- test/sync/withhold-transition.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All withhold-transition tests pass
    - No I/O or side effects in module
    - Dedup keys match D-13 colon-separated format
  </acceptance_criteria>
  <done>GREEN: SYNC-15 transition logic tested</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | Third-party dependency enters supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-SC | Tampering | npm install nodemailer | mitigate | Blocking human-verify checkpoint before install (RESEARCH Package Legitimacy Audit) |
| T-04-01 | Spoofing | nodemailer package | mitigate | Verify npm + GitHub publisher match at checkpoint |
</threat_model>

<verification>
npm test -- test/sync/withhold-transition.test.ts && npm run typecheck
</verification>

<success_criteria>
computeWithholdTransition passes all episode/dedup/suppression tests; nodemailer installed with human-approved legitimacy check.
</success_criteria>

<output>
Create `.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-01-SUMMARY.md` when done
</output>
