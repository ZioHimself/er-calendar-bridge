---
phase: 04-microsoft-graph-writer-and-withhold-notifications
plan: 06
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-05
files_modified:
  - src/index.ts
  - test/cli/audit-list.test.ts
  - README.md
autonomous: true
requirements:
  - SYNC-16
  - OPS-03
user_setup: []

must_haves:
  truths:
    - "Operator runs er-calendar-bridge audit list to inspect withhold history (D-15, SYNC-16)"
    - "audit list --since ISO-8601 filters rows at or after timestamp (D-15)"
    - "CLI output shows uid, recurrenceId, tier, propagation, notify_status, recorded_at — not event titles (D-16, OPS-03)"
    - "Usage text documents sync and audit subcommands"
  artifacts:
    - path: src/index.ts
      provides: audit list subcommand
    - path: test/cli/audit-list.test.ts
      provides: CLI smoke via spawn or exported handler
    - path: README.md
      provides: operator audit inspect instructions
  key_links:
    - from: src/index.ts
      to: src/store/audit-store.ts
      via: listAuditSince
      pattern: listAuditSince
---

<objective>
Expose operator-facing `audit list [--since]` CLI reading SQLite withhold audit — completing OPS-03 inspectability without HTTP UI.

Purpose: SYNC-16 IT/operator access via CLI per D-15; document pilot usage.
Output: Audit subcommand, CLI test, README section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-CONTEXT.md (D-15, D-16, D-07)
@.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-PATTERNS.md (index.ts CLI section)
@src/index.ts
@src/store/audit-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): audit list CLI tests</name>
  <read_first>
    src/index.ts
    test/integration/sync-cycle.test.ts
    src/store/audit-store.ts
  </read_first>
  <files>test/cli/audit-list.test.ts</files>
  <behavior>
    - Seed audit store with two rows at different recorded_at; audit list prints both
    - audit list --since filters to newer row only
    - Invalid --since value exits non-zero with clear message (Zod or Date.parse validation)
    - Output columns include notify_status and dedup_key; output does not contain a seeded forbidden title string (D-16)
  </behavior>
  <action>
    Create test/cli/audit-list.test.ts.

    Prefer testing via exported runCli(argv) or main() refactor if index.ts is hard to spawn — extract minimal dispatch function if needed without changing sync behavior.

    Seed data using openAuditStore(':memory:') or temp sqlite file with appendAuditRow.

    Commit RED; verify failure.
  </action>
  <verify>
    <automated>npm test -- test/cli/audit-list.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover D-15 since filter semantics
    - No HTTP server invoked
  </acceptance_criteria>
  <done>RED: audit CLI tests</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): audit list subcommand and README</name>
  <read_first>
    test/cli/audit-list.test.ts
    README.md
    .planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-VALIDATION.md
  </read_first>
  <files>src/index.ts, README.md</files>
  <action>
    Extend argv dispatch in src/index.ts:

    - `audit list` optional `--since <iso>` — loadConfig, openAuditStore(sqlitePath), listAuditSince, print human-readable lines (tsv or fixed columns) to stdout.

    - Update usage when unknown subcommand: document `sync [--watch]` and `audit list [--since ISO-8601]`.

    - Parse --since strictly; invalid ISO → exit 1 with stderr message (V5 input validation).

    - Do not add HTTP health endpoint (D-15).

    Update README Operator section: how to inspect withhold audit after sync; NOTIFY_OWNER_EMAIL optional; link TAG_GUIDANCE_URL purpose (D-12).

    Run audit CLI tests and full validation gate.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - audit list tests pass
    - README documents audit list for OPS-03 withhold visibility
    - CLI help reflects both subcommands
  </acceptance_criteria>
  <done>OPS-03 operator audit inspect complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → SQLite | Local read of withhold audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Information disclosure | CLI stdout | mitigate | D-16: list only audit columns; no title field in schema |
| T-04-15 | Tampering | --since input | mitigate | Validate ISO-8601 before SQL query |
| T-04-16 | Information disclosure | audit list on shared host | accept | Pilot single-operator laptop; local CLI per OPS-04 |
</threat_model>

<verification>
npm test && npm run typecheck && npm run lint
</verification>

<success_criteria>
Operator can query withhold audit from CLI; phase 4 gate commands green.
</success_criteria>

<output>
Create `.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-06-SUMMARY.md` when done
</output>
