---
phase: 04-microsoft-graph-writer-and-withhold-notifications
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/store/schema.sql
  - src/store/audit-store.ts
  - test/store/audit-store.test.ts
autonomous: true
requirements:
  - SYNC-16
  - OPS-03
user_setup: []

must_haves:
  truths:
    - "Operator can list withhold audit rows filtered by --since timestamp (D-15, SYNC-16, OPS-03)"
    - "Audit rows contain timestamp, uid, recurrenceId, tier, propagation, notify_status, dedup_key, optional error — no title/summary (D-16)"
    - "withhold_notify_state persists bridge_uuid, last_propagation, episode per source instance (D-13 discretion)"
    - "First-sight drop can persist bridge_uuid without google_event_id mapping (RESEARCH Pitfall 1)"
  artifacts:
    - path: src/store/audit-store.ts
      provides: openAuditStore with state upsert + audit append + listSince
      exports: ["openAuditStore", "AuditStore", "NotifyStatus"]
    - path: src/store/schema.sql
      provides: withhold_notify_state and withhold_audit tables
      contains: "withhold_audit"
    - path: test/store/audit-store.test.ts
      provides: in-memory store tests
  key_links:
    - from: src/store/audit-store.ts
      to: src/store/schema.sql
      via: loadSchema on open
      pattern: loadSchema
    - from: src/store/audit-store.ts
      to: src/store/mapping-store.ts
      via: normalizeRecurrenceKey pattern
      pattern: normalizeRecurrenceKey
---

<objective>
Add SQLite withhold notify state and append-only audit log following mapping-store patterns, with PII-free columns and query-by-since for operator CLI.

Purpose: SYNC-16 and OPS-03 require durable withhold history; D-16 forbids event titles at rest.
Output: `audit-store.ts`, extended `schema.sql`, passing store unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-CONTEXT.md (D-13, D-15, D-16)
@.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-PATTERNS.md (audit-store, schema.sql)
@src/store/mapping-store.ts
@src/store/schema.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Audit store schema and behavior tests</name>
  <read_first>
    test/store/mapping-store.test.ts
    src/store/mapping-store.ts
    .planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-RESEARCH.md (Pattern 3)
  </read_first>
  <files>test/store/audit-store.test.ts</files>
  <behavior>
    - openAuditStore(':memory:') creates withhold_notify_state and withhold_audit tables
    - upsertNotifyState for uid+recurrenceId stores bridge_uuid, last_propagation, episode
    - appendAuditRow inserts row with notify_status enum sent|skipped|failed|disabled (D-16)
    - listAuditSince(iso) returns rows with recorded_at >= since ordered ascending
    - Audit row columns never include summary/title fields — test inserts only allowed fields
    - getNotifyState returns undefined for unknown uid
    - Master event uses recurrence_id '' matching mapping-store convention
  </behavior>
  <action>
    Create test/store/audit-store.test.ts importing openAuditStore from src/store/audit-store.js (missing until Task 2).

    Use afterEach store.close() pattern from mapping-store tests.

    Commit RED; verify failure.
  </action>
  <verify>
    <automated>npm test -- test/store/audit-store.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Tests encode D-16 field set and OPS-03 listability contract
    - No fixture event titles passed into store APIs in tests
  </acceptance_criteria>
  <done>RED: audit-store tests committed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement openAuditStore and DDL</name>
  <read_first>
    test/store/audit-store.test.ts
    src/store/mapping-store.ts
    .planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-PATTERNS.md
  </read_first>
  <files>src/store/schema.sql, src/store/audit-store.ts</files>
  <action>
    Extend src/store/schema.sql with:

    withhold_notify_state (PK source_uid, recurrence_id; columns bridge_uuid, last_propagation, episode INTEGER, updated_at TEXT).

    withhold_audit append-only (id or rowid, recorded_at TEXT, source_uid, recurrence_id, tier TEXT, propagation TEXT, notify_status TEXT, dedup_key TEXT, error TEXT NULL). Index on recorded_at. Optional UNIQUE on dedup_key to enforce SYNC-15 at DB layer.

    Implement src/store/audit-store.ts mirroring mapping-store: loadSchema, normalizeRecurrenceKey (copy or import shared helper — prefer duplicate small function to avoid scope creep), openAuditStore(sqlitePath), methods getNotifyState, upsertNotifyState, appendAuditRow, listAuditSince, close.

    Export type NotifyStatus = 'sent' | 'skipped' | 'failed' | 'disabled'.

    Run audit-store tests until green.
  </action>
  <verify>
    <automated>npm test -- test/store/audit-store.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All audit-store tests pass
    - Existing mapping-store tests still pass (shared schema.sql)
    - listAuditSince suitable for CLI audit list (D-15)
  </acceptance_criteria>
  <done>GREEN: SYNC-16 persistence layer ready</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite file on disk | Local withhold metadata; no network exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information disclosure | withhold_audit rows | mitigate | D-16: no summary/title columns; tests assert schema |
| T-04-03 | Tampering | audit table | mitigate | Append-only API — no update/delete methods in Phase 4 |
</threat_model>

<verification>
npm test -- test/store/audit-store.test.ts && npm test -- test/store/mapping-store.test.ts
</verification>

<success_criteria>
Audit and notify state persist in shared bridge.db schema; operator-queryable listSince API exists.
</success_criteria>

<output>
Create `.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-02-SUMMARY.md` when done
</output>
