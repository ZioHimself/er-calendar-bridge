---
phase: 04-microsoft-graph-writer-and-withhold-notifications
plan: 04
type: tdd
wave: 2
depends_on:
  - 04-03
files_modified:
  - src/notify/withhold-notifier.ts
  - test/notify/withhold-notifier.test.ts
autonomous: true
requirements:
  - SYNC-14
user_setup: []

must_haves:
  truths:
    - "Owner email uses fixed static subject line; details only in body (D-11)"
    - "Email body is English, minimal, explains ER-PUBLIC/ER-INTERNAL/ER-SENSITIVE and optional doc link (D-09, D-10, D-12)"
    - "Email body never contains fixture title, location, description, or attendees (D-09)"
    - "No IT BCC recipient on outbound mail (D-07)"
    - "SMTP send failure returns failed status without throwing to caller (D-08)"
    - "When notifyOwnerEmail unset, factory returns no-op notifier that does not call transport (D-05)"
  artifacts:
    - path: src/notify/withhold-notifier.ts
      provides: createWithholdNotifier
      exports: ["createWithholdNotifier"]
    - path: test/notify/withhold-notifier.test.ts
      provides: jsonTransport / mock coverage
  key_links:
    - from: src/notify/withhold-notifier.ts
      to: src/notify/types.ts
      via: implements WithholdNotifier
      pattern: WithholdNotifier
    - from: src/notify/withhold-notifier.ts
      to: nodemailer
      via: createTransport sendMail
      pattern: createTransport
---

<objective>
Implement production WithholdNotifier using nodemailer with minimal English template and CI-safe mocked transport tests.

Purpose: SYNC-14 owner notification content and D-07/D-09/D-11/D-12 email policy.
Output: `withhold-notifier.ts` and unit tests — no sync loop wiring yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-CONTEXT.md (D-05, D-07, D-08, D-09, D-10, D-11, D-12)
@.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-RESEARCH.md (Pattern 2, Pitfall 4 TLS)
@src/notify/types.ts
@src/config/env.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Withhold notifier behavior tests</name>
  <read_first>
    test/integration/sync-cycle.test.ts
    .planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-RESEARCH.md (nodemailer jsonTransport)
    src/notify/types.ts
  </read_first>
  <files>test/notify/withhold-notifier.test.ts</files>
  <behavior>
    - createWithholdNotifier with notifyOwnerEmail unset: notifyWithhold resolves without sendMail call (D-05)
    - With notify email + nodemailer jsonTransport: sendMail called once with fixed subject string (D-11)
    - Body contains tier and propagation labels; contains ER-PUBLIC, ER-INTERNAL, ER-SENSITIVE guidance text (D-10)
    - Body does not contain known fixture strings like a test event summary passed in params — params must not include summary
    - tagGuidanceUrl set: body includes URL; unset: no broken placeholder link (D-12)
    - Transport reject: returns status failed, does not throw (D-08)
    - Port 587 config uses secure false; port 465 uses secure true (Pitfall 4)
  </behavior>
  <action>
    Create test/notify/withhold-notifier.test.ts importing createWithholdNotifier — module missing until Task 2.

    Use vi.spyOn or inject mock transporter factory if needed for RED isolation.

    Commit RED; verify tests fail.
  </action>
  <verify>
    <automated>npm test -- test/notify/withhold-notifier.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Tests enforce D-09 PII-free email content
    - No live SMTP in tests (TEST-05)
  </acceptance_criteria>
  <done>RED: notifier tests committed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): createWithholdNotifier implementation</name>
  <read_first>
    test/notify/withhold-notifier.test.ts
    .planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-PATTERNS.md (withhold-notifier section)
  </read_first>
  <files>src/notify/withhold-notifier.ts</files>
  <action>
    Implement createWithholdNotifier(config: Pick AppConfig, 'notifyOwnerEmail' | 'tagGuidanceUrl' | smtp fields>) returning WithholdNotifier.

    When notifyOwnerEmail missing/empty: return async () => ({ status: 'sent' }) no-op (audit layer will use notify_status disabled — wired in 04-05).

    Otherwise build nodemailer transport: secure only when smtpPort === 465; auth when smtpUser provided.

    buildMinimalBody helper: English plain text; static subject constant exported or internal for tests; include propagation (busy vs drop) and tier; tag guidance bullets; append TAG_GUIDANCE_URL line only when non-empty (D-12).

    Single recipient to notifyOwnerEmail only — no bcc (D-07).

    Wrap sendMail in try/catch; log is caller responsibility — return { status: 'failed' } on error without throw (D-08).

    Run notifier tests until green.
  </action>
  <verify>
    <automated>npm test -- test/notify/withhold-notifier.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All notifier unit tests pass
    - Implements WithholdNotifier from types.ts
    - Subject line identical across busy and drop sends (D-11)
  </acceptance_criteria>
  <done>GREEN: SYNC-14 SMTP notifier ready</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bridge → SMTP relay | Withhold metadata leaves process; must stay minimal (D-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information disclosure | email body | mitigate | D-09 minimal body; unit tests grep forbidden fixture titles |
| T-04-08 | Information disclosure | SMTP auth errors | mitigate | D-08 catch errors; redact password in logs at index.ts (04-06) |
| T-04-09 | Spoofing | mail From/To | mitigate | SMTP_FROM + single NOTIFY_OWNER_EMAIL from config (D-06) |
</threat_model>

<verification>
npm test -- test/notify/withhold-notifier.test.ts
</verification>

<success_criteria>
WithholdNotifier sends minimal English mail when configured; no-op when notify email unset.
</success_criteria>

<output>
Create `.planning/phases/04-microsoft-graph-writer-and-withhold-notifications/04-04-SUMMARY.md` when done
</output>
