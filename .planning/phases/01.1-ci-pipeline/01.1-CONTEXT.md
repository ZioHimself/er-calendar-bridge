# Phase 01.1: CI pipeline - Context

**Gathered:** 2026-09-13
**Status:** Ready for planning

<domain>
## Phase Boundary

Add GitHub Actions CI immediately after the Phase 1 scaffold — running test, lint, typecheck, and build on every push to `main` — so all subsequent business-logic and sync work stays merge-ready without live provider credentials.

This phase delivers the CI workflow and any supporting config changes (e.g., `tsconfig.test.json`). No application logic, Docker packaging, or provider integration.

</domain>

<decisions>
## Implementation Decisions

### Formatting Enforcement
- **D-01:** Stay eslint-only — carry forward Phase 1 decision D-12. No Prettier dependency or `format:check` script.
- **D-02:** No `format:fix` script. Developers rely on editor formatting or manual fixes.
- **D-03:** No git pre-commit hooks (no husky, no lint-staged). CI is the quality gate per OPS-04 minimum-viable tooling.

### Test File Typechecking
- **D-04:** Add `tsconfig.test.json` extending the base config to include `test/**/*.ts`. Same strict compiler options as `src/`.
- **D-05:** Single `npm run typecheck` command covers both `src/` and `test/` (e.g., `tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json`, or equivalent project-reference approach).
- **D-06:** Test files use identical strict TypeScript settings to application code — no relaxed rules in `test/`.
- **D-07:** Primary motivation: catch type errors in test helpers and fixture tests before they reach `main`.

### Workflow Job Layout
- **D-08:** Parallel CI jobs — separate jobs for `test`, `lint`, `typecheck`, and `build`. Clear failure attribution; Phase 5 adds a `docker-build` job alongside without restructuring.
- **D-09:** CI triggers on `push` and `pull_request` targeting `main` only. `pull_request` trigger is configured for future adoption but the team uses trunk-based development with direct pushes to `main` for now.
- **D-10:** Node.js 24 only in CI — matches `engines.node: "24.x"` (Phase 1 D-09). No version matrix.
- **D-11:** Cache npm dependencies via `actions/setup-node` with `cache: npm`. Use `npm ci` (not `npm install`) for reproducible installs from committed `package-lock.json`.

### Build Step
- **D-12:** Include `npm run build` as a separate parallel CI job now — verifies `tsc` emit to `dist/`, not just `--noEmit` typecheck.
- **D-13:** No `dist/` artifact upload. Build job verifies compilation only; Phase 5 Docker job builds from source.
- **D-14:** Build failure fails the CI workflow the same as test/lint/typecheck. No branch protection rules or required status checks — trunk-based workflow without PRs means merge blocking is not configured.

### Development Workflow Documentation
- **D-15:** Document trunk-based development in README: direct pushes to `main`, no PR workflow for now, CI runs on every push to `main`. Do not document branch protection setup or CI status badges.

### Claude's Discretion
None — all discussed areas had explicit user choices.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Requirements & Architecture (external — NGO IT strategy repo)
- `it-strategy/er-calendar-bridge/calendar-sync-02-requirements.md` — TEST-05 (CI without live credentials), OPS-04 (minimum viable tooling)
- `it-strategy/er-calendar-bridge/calendar-sync-03-architecture.md` — Deployment model; Phase 5 Docker CI extension

### Project Planning (in-repo)
- `.planning/PROJECT.md` — Core value, constraints (TypeScript strict, 7-person team scale)
- `.planning/REQUIREMENTS.md` — TEST-05, OPS-04 traceability; TEST-05 also referenced in Phase 5
- `.planning/ROADMAP.md` — Phase 01.1 goal, success criteria (5 items), Phase 5 Docker CI extension note

### Prior Phase Context (in-repo)
- `.planning/phases/01-project-scaffold-and-test-harness/01-CONTEXT.md` — D-10 (npm + lockfile), D-11 (scripts), D-12 (eslint-only), D-09 (Node 24), test typecheck deferral

### Research (in-repo)
- `.planning/research/STACK.md` — Node 24 LTS, vitest, eslint toolchain
- `.planning/research/SUMMARY.md` — Phase 01.1 listed as GitHub Actions test/lint/typecheck

### Existing Tooling (in-repo — CI will exercise these)
- `package.json` — scripts: `test`, `lint`, `typecheck`, `build`; `engines.node: "24.x"`
- `tsconfig.json` — strict config, `include: ["src/**/*.ts"]`, `exclude: ["test"]`
- `eslint.config.mjs` — flat eslint config (eslint-only, no Prettier)
- `vitest.config.ts` — test runner config
- `package-lock.json` — committed for `npm ci` reproducibility

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `package.json` scripts already match ROADMAP success criteria: `npm test`, `npm run lint`, `npm run typecheck`, `npm run build`.
- `package-lock.json` committed (Phase 1 D-10) — ready for `npm ci` in CI.
- No `.github/workflows/` directory exists yet — greenfield CI setup.

### Established Patterns
- Eslint-only linting (no Prettier) — OPS-04 minimum viable tooling constraint.
- Main `tsconfig.json` excludes `test/` — Phase 1 accepted MVP; Phase 01.1 adds `tsconfig.test.json` to close the gap.
- `vitest run` (not watch) — CI-safe test script already configured.
- Node 24 LTS target — local dev on Node 22 triggers EBADENGINE warnings; CI pins Node 24.

### Integration Points
- `.github/workflows/ci.yml` (or similar) — new file, primary deliverable.
- `tsconfig.test.json` — new file, extends base tsconfig for test typechecking.
- `README.md` — add trunk-based development + CI section (no branch protection docs).
- Phase 5 will add a `docker-build` job to the same workflow file without restructuring (ROADMAP success criterion #5).

</code_context>

<specifics>
## Specific Ideas

- Trunk-based development: push directly to `main`, no PRs for now. CI still includes `pull_request` trigger on `main` for future workflow adoption.
- No time spent on branch protection, required status checks, or CI badges — not applicable without PRs.
- User explicitly rejected Prettier and pre-commit hooks — keep tooling lean.
- Test typechecking is a priority because test helpers and fixture tests are substantial and will grow in Phase 2.

</specifics>

<deferred>
## Deferred Ideas

- **Prettier / code formatter** — explicitly rejected; stay eslint-only.
- **Git pre-commit hooks (husky + lint-staged)** — rejected; CI is the gate.
- **Branch protection documentation** — not needed for trunk-based workflow without PRs.
- **CI status badge in README** — deferred; not useful without PR workflow.
- **dist/ artifact upload** — deferred to Phase 5 if Docker job needs cached build output.
- **Node 22 compatibility matrix** — rejected; CI pins Node 24 only.
- **Docker build job** — Phase 5 scope per ROADMAP.

</deferred>

---

*Phase: 01.1-CI pipeline*
*Context gathered: 2026-09-13*
