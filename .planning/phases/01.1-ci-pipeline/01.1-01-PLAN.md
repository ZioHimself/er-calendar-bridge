---
phase: 01.1-ci-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.test.json
  - package.json
autonomous: true
requirements:
  - TEST-05
  - OPS-04
user_setup: []

must_haves:
  truths:
    - "D-01: No Prettier or format:check added — eslint-only per Phase 1 carry-forward"
    - "D-02: No format:fix script added — developers use editor formatting"
    - "D-04: tsconfig.test.json extends base config and includes test/**/*.ts"
    - "D-05: npm run typecheck runs tsc on both tsconfig.json and tsconfig.test.json"
    - "D-06: test/ uses identical strict compiler options as src/ — no relaxed rules"
    - "D-07: type errors in test helpers and fixture tests are caught before merge"
    - "npm run typecheck exits 0 on current test tree"
    - "Base tsconfig.json remains src-only for build emit — unchanged include/exclude"
  artifacts:
    - path: tsconfig.test.json
      provides: "Strict typecheck config for test/**/*.ts"
      contains: '"extends": "./tsconfig.json"'
    - path: package.json
      provides: "Dual tsc typecheck script"
      contains: "tsconfig.test.json"
  key_links:
    - from: package.json
      to: tsconfig.test.json
      via: "typecheck script second tsc invocation"
      pattern: "tsconfig\\.test\\.json"
    - from: tsconfig.test.json
      to: tsconfig.json
      via: "extends base strict config"
      pattern: '"extends": "./tsconfig.json"'
    - from: test/helpers/parse-ics.ts
      to: tsconfig.test.json
      via: "test imports src with .js extensions"
      pattern: "src/adapters/ical"
---

<objective>
Close the Phase 1 test-typecheck gap by adding `tsconfig.test.json` and updating the `typecheck` npm script to cover both `src/` and `test/` with identical strict settings.

Purpose: Catch type errors in test helpers and fixture tests before they reach `main` — critical before Phase 2 test growth (D-07).
Output: `tsconfig.test.json` committed; `npm run typecheck` runs dual `tsc --noEmit`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-ci-pipeline/01.1-CONTEXT.md
@.planning/phases/01.1-ci-pipeline/01.1-RESEARCH.md
@.planning/phases/01.1-ci-pipeline/01.1-PATTERNS.md

## Locked decisions implemented in this plan
- D-04: Add `tsconfig.test.json` extending base config for `test/**/*.ts`
- D-05: Single `npm run typecheck` covers both src and test via dual tsc
- D-06: Same strict compiler options as application code — no relaxed rules
- D-07: Primary motivation — catch test helper type errors pre-merge

## Interfaces from existing codebase

From tsconfig.json (do NOT change include/exclude for build):
- `rootDir: "src"`, `strict: true`, `noUncheckedIndexedAccess: true`
- `include: ["src/**/*.ts"]`, `exclude: ["node_modules", "dist", "test"]`

From package.json (current typecheck — to be updated):
- `"typecheck": "tsc --noEmit"` → dual tsc per D-05

From test/helpers/parse-ics.ts (import pattern tsconfig.test.json must support):
- Imports from `../../src/adapters/ical/index.js` with `.js` extension (NodeNext)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tsconfig.test.json for strict test typechecking</name>
  <read_first>
    .planning/phases/01.1-ci-pipeline/01.1-CONTEXT.md (D-04, D-06, D-07)
    .planning/phases/01.1-ci-pipeline/01.1-RESEARCH.md (Pattern 2 — dual tsconfig, rootDir override)
    .planning/phases/01.1-ci-pipeline/01.1-PATTERNS.md (tsconfig.test.json section)
    tsconfig.json
    test/helpers/parse-ics.ts
  </read_first>
  <files>tsconfig.test.json</files>
  <action>
    Create `tsconfig.test.json` at repo root per D-04 and RESEARCH Pattern 2:

    - `"extends": "./tsconfig.json"` — inherits all strict compiler options from base (D-06)
    - `"compilerOptions": { "rootDir": ".", "noEmit": true }` — override rootDir to avoid TS6059 when test files import from `src/` (RESEARCH Pitfall 2)
    - `"include": ["test/**/*.ts"]`
    - `"exclude": ["node_modules", "dist"]`

    Do NOT modify `tsconfig.json` — base config must remain src-only for `npm run build` emit to `dist/`. Do not add relaxed compiler options (no `strict: false`, no skipped checks). Do not add `tsconfig.test.json` to base include array.
  </action>
  <verify>
    <automated>test -f tsconfig.test.json && node -e "const c=require('./tsconfig.test.json'); const b=require('./tsconfig.json'); if(!c.extends||c.compilerOptions?.rootDir!=='.'||c.compilerOptions?.noEmit!==true||!c.include?.includes('test/**/*.ts')) process.exit(1); if(!b.exclude?.includes('test')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.test.json` exists at repo root
    - File extends `./tsconfig.json` and sets `compilerOptions.rootDir` to `"."`
    - File sets `compilerOptions.noEmit` to `true`
    - File includes `test/**/*.ts` only
    - Base `tsconfig.json` is unchanged (still excludes `test`)
  </acceptance_criteria>
  <done>Test typecheck config exists with rootDir override; strict options inherited from base per D-04 and D-06</done>
</task>

<task type="auto">
  <name>Task 2: Update typecheck script and verify dual tsc passes</name>
  <read_first>
    .planning/phases/01.1-ci-pipeline/01.1-CONTEXT.md (D-05, D-07)
    .planning/phases/01.1-ci-pipeline/01.1-RESEARCH.md (updated typecheck script example)
    package.json
    tsconfig.test.json
    tsconfig.json
  </read_first>
  <files>package.json</files>
  <action>
    Update `package.json` `scripts.typecheck` per D-05 to exactly:

    `"typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json"`

    Leave all other scripts unchanged (`test`, `lint`, `build`). Do NOT add Prettier, `format:check`, `format:fix`, husky, or lint-staged per D-01, D-02, D-03.

    Run `npm run typecheck` and confirm both invocations succeed on the current test tree (including `test/helpers/` and `test/smoke.test.ts`).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` typecheck script runs dual tsc with explicit `-p` flags for both configs
    - `npm run typecheck` exits 0
    - `tsc --noEmit -p tsconfig.test.json` alone exits 0 (no TS6059 rootDir errors)
    - No new npm dependencies added
    - `test`, `lint`, and `build` scripts unchanged
  </acceptance_criteria>
  <done>D-05 satisfied: single typecheck command covers src/ and test/ with strict settings per D-06 and D-07</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tsc → test/ filesystem | Compiler reads test sources and resolves src/ imports — no network |
| package.json → CI | typecheck script is the CI contract for typecheck job |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Tampering | tsconfig.test.json | mitigate | Extends base strict config; no relaxed compiler options per D-06 |
| T-01.1-02 | Elevation | strict bypass in test config | mitigate | Verify `extends` inherits `strict: true` and `noUncheckedIndexedAccess: true` from base |
</threat_model>

<verification>
After plan 01.1-01:
- `npm run typecheck` exits 0 covering both src/ and test/
- `tsconfig.json` unchanged for build emit
</verification>

<success_criteria>
1. `tsconfig.test.json` committed with rootDir override and strict inheritance (D-04, D-06)
2. `npm run typecheck` runs dual tsc per D-05
3. Test helper and smoke test files typecheck cleanly (D-07)
4. No new npm packages or formatting tooling added (OPS-04, D-01–D-03)
</success_criteria>

<output>
Create `.planning/phases/01.1-ci-pipeline/01.1-01-SUMMARY.md` when done
</output>
