---
phase: 01.1-ci-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 01.1-01
  - 01.1-02
files_modified:
  - README.md
autonomous: true
requirements:
  - TEST-05
  - OPS-04
user_setup: []

must_haves:
  truths:
    - "D-15: README documents trunk-based development with direct pushes to main"
    - "D-15: README states CI runs on every push to main"
    - "D-14: README does NOT document branch protection or required status checks"
    - "Deferred: README has no CI status badges"
    - "Developer can find local pre-push checklist in README"
    - "Phase 01.1 full gate passes: test, lint, typecheck, build all green"
    - "ROADMAP success criterion #2 superseded by CONTEXT — trunk-based docs instead of branch protection"
  artifacts:
    - path: README.md
      provides: "Development workflow and CI documentation"
      contains: "main"
  key_links:
    - from: README.md
      to: .github/workflows/ci.yml
      via: "documents CI behavior on push to main"
      pattern: "CI|push"
    - from: README.md
      to: package.json
      via: "local pre-push checklist references npm scripts"
      pattern: "npm (test|run)"
---

<objective>
Document trunk-based development workflow in README and run the Phase 01.1 validation gate confirming all deliverables integrate cleanly.

Purpose: Satisfy D-15 (human process docs) and close the phase with a green local gate before first CI push.
Output: README development section; all four npm scripts exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.1-ci-pipeline/01.1-CONTEXT.md
@.planning/phases/01.1-ci-pipeline/01.1-RESEARCH.md (Pattern 3 — trunk-based README)
@.planning/phases/01.1-ci-pipeline/01.1-PATTERNS.md (README section)
@.planning/phases/01.1-ci-pipeline/01.1-01-SUMMARY.md
@.planning/phases/01.1-ci-pipeline/01.1-02-SUMMARY.md

## Locked decisions implemented in this plan
- D-15: Document trunk-based development — direct pushes to main, CI on every push
- D-14: Do NOT document branch protection or required status checks
- Deferred: No CI status badges in README

## ROADMAP drift note
ROADMAP success criterion #2 ("branch protection documented in README") conflicts with D-14/D-15. Implement per CONTEXT — trunk-based workflow docs, not branch protection.

## Prior plan outputs required
- `tsconfig.test.json` and dual tsc typecheck script (plan 01.1-01)
- `.github/workflows/ci.yml` with four jobs (plan 01.1-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add trunk-based development and CI section to README</name>
  <read_first>
    .planning/phases/01.1-ci-pipeline/01.1-CONTEXT.md (D-14, D-15, deferred ideas)
    .planning/phases/01.1-ci-pipeline/01.1-RESEARCH.md (Pattern 3 — suggested content outline)
    .planning/phases/01.1-ci-pipeline/01.1-PATTERNS.md (README section pattern)
    README.md
    package.json
    .github/workflows/ci.yml
  </read_first>
  <files>README.md</files>
  <action>
    Add a `## Development` section to README.md before the `## Status` section, matching existing heading style (concise prose, optional table). Per D-15, include:

    1. **Trunk-based workflow:** Team pushes directly to `main`; PR workflow not used for now. `pull_request` trigger exists in CI for future adoption (D-09).
    2. **CI behavior:** GitHub Actions runs on every push to `main` (and on PRs targeting `main` when used). Four parallel jobs: test, lint, typecheck, build.
    3. **Local pre-push checklist:** `npm test && npm run lint && npm run typecheck && npm run build`
    4. **Node version:** CI uses Node.js 24 (matches `engines.node` in package.json). Brief note that local Node 22 may show EBADENGINE warnings; recommend nvm/fnm for alignment — one sentence only per OPS-04.

    Explicitly do NOT add per D-14 and deferred ideas:
    - Branch protection setup instructions
    - Required status checks configuration
    - CI status badge images or shields.io links
    - Prettier or husky setup instructions
    - Docker build documentation (Phase 5)

    Keep section lean — match README tone elsewhere; no lengthy setup guide.
  </action>
  <verify>
    <automated>grep -qi 'main' README.md && grep -qiE 'trunk|push' README.md && ! grep -qiE 'branch protection|required status|badge|shields\.io|prettier|husky' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README has `## Development` section before `## Status`
    - Section mentions direct pushes to `main` (trunk-based workflow)
    - Section mentions CI runs on push to `main` with four jobs
    - Section includes local pre-push command with all four npm scripts
    - Section mentions Node 24 for CI
    - README contains no branch protection, status badge, Prettier, or husky references
  </acceptance_criteria>
  <done>D-15 satisfied: trunk-based development and CI behavior documented without branch protection per D-14</done>
</task>

<task type="auto">
  <name>Task 2: Run Phase 01.1 validation gate</name>
  <read_first>
    .planning/ROADMAP.md (Phase 01.1 success criteria)
    .planning/phases/01.1-ci-pipeline/01.1-VALIDATION.md (sampling rate, wave gate)
    .planning/phases/01.1-ci-pipeline/01.1-CONTEXT.md (all D-01 through D-15)
    package.json
    tsconfig.test.json
    .github/workflows/ci.yml
    README.md
  </read_first>
  <files>README.md</files>
  <action>
    Run Phase 01.1 validation gate per 01.1-VALIDATION.md:

    1. `npm test` — fixture-only vitest (TEST-05: no credentials)
    2. `npm run lint` — eslint flat config (D-01: eslint-only)
    3. `npm run typecheck` — dual tsc on src/ and test/ (D-05)
    4. `npm run build` — tsc emit to dist/ (D-12)

    Structurally verify phase deliverables exist:
    - `tsconfig.test.json` with rootDir override
    - `.github/workflows/ci.yml` with four named jobs
    - README development section

    Verify ROADMAP success criteria against CONTEXT decisions:
    - Criterion 1: CI workflow runs all four scripts — satisfied by ci.yml
    - Criterion 2: ROADMAP mentions branch protection — implement per CONTEXT D-14/D-15 instead (trunk-based docs, no protection docs)
    - Criterion 3: No live credentials — fixture-only tests
    - Criterion 4: Proportionate infra — four jobs, two actions, no matrix
    - Criterion 5: Phase 5 docker-build extension point — four sibling jobs in ci.yml

    Fix only if gate fails. Do not add scope beyond Phase 01.1.
  </action>
  <verify>
    <automated>npm test && npm run lint && npm run typecheck && npm run build && test -f tsconfig.test.json && test -f .github/workflows/ci.yml && test -f dist/index.js</automated>
  </verify>
  <acceptance_criteria>
    - All four npm scripts exit 0 in sequence
    - `dist/index.js` exists after build
    - `tsconfig.test.json`, `.github/workflows/ci.yml`, and README development section all present
    - No new npm dependencies beyond Phase 1
    - Phase 01.1 goal met: CI + typecheck config + docs ready for first GitHub push
  </acceptance_criteria>
  <done>Phase 01.1 complete: TEST-05 and OPS-04 satisfied; all locked decisions D-01 through D-15 implemented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → developer | Process documentation only — no secrets or credentials |
| Local gate → CI | Local scripts must match CI job commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-06 | Information Disclosure | README content | mitigate | No credential examples; reference .env.example for config keys only |
| T-01.1-07 | Tampering | Validation bypass | mitigate | Gate requires all four scripts; no strict mode or test skips |
</threat_model>

<verification>
Phase 01.1 complete when:
```bash
npm test && npm run lint && npm run typecheck && npm run build
```
All exit 0. Manual: first push to GitHub confirms Actions tab shows four green jobs.
</verification>

<success_criteria>
1. README documents trunk-based workflow per D-15
2. No branch protection or CI badge docs per D-14 and deferred ideas
3. Full local gate green (TEST-05, OPS-04)
4. All Phase 01.1 artifacts present and integrated
5. Phase ready for `/gsd:execute-phase 2` after CI first-run verification
</success_criteria>

<output>
Create `.planning/phases/01.1-ci-pipeline/01.1-03-SUMMARY.md` when done
</output>
