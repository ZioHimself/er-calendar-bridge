# Phase 01.1: CI pipeline - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-13
**Phase:** 01.1-ci pipeline
**Areas discussed:** Formatting enforcement, Test file typechecking, Workflow job layout, Build step in CI

---

## Formatting Enforcement

| Option | Description | Selected |
|--------|-------------|----------|
| Eslint-only | Keep Phase 1 D-12. No Prettier. CI runs lint only. | ✓ |
| Add Prettier + format check | Add Prettier devDep with `npm run format:check` in CI. | |
| Eslint stylistic rules | No Prettier, upgrade eslint with formatting-related rules. | |

**User's choice:** Eslint-only
**Notes:** Format scope, fix script, and pre-commit hooks all N/A or rejected. No `format:fix` script. No husky/lint-staged — CI is the gate.

---

## Test File Typechecking

| Option | Description | Selected |
|--------|-------------|----------|
| Add tsconfig.test.json | Extend base config to include test/. CI typechecks both src/ and test/. | ✓ |
| Vitest-only — no test typecheck | Keep Phase 1 MVP. CI typechecks src/ only. | |
| Merge test/ into main tsconfig | Remove test/ exclusion from tsconfig.json. | |

| Option | Description | Selected |
|--------|-------------|----------|
| Single npm run typecheck covers both | One command for src/ and test/. | ✓ |
| Split: typecheck + typecheck:test | Separate scripts, CI runs both. | |
| Use vitest --typecheck | Vitest 5 experimental typecheck mode. | |

| Option | Description | Selected |
|--------|-------------|----------|
| Same strict settings | test/ extends base with identical strict flags. | ✓ |
| Relaxed for test helpers | Looser rules in test/. | |
| You decide | Planner picks based on what compiles. | |

**User's choice:** Add tsconfig.test.json, single typecheck script, same strict settings
**Notes:** Primary motivation: catch type errors in test code before merge.

---

## Workflow Job Layout

| Option | Description | Selected |
|--------|-------------|----------|
| Parallel jobs | Separate jobs: test, lint, typecheck, build. Phase 5 adds docker-build alongside. | ✓ |
| Single job, sequential steps | One job runs all checks in order. | |
| Matrix job | One job matrix over [test, lint, typecheck]. | |

| Option | Description | Selected |
|--------|-------------|----------|
| push + pull_request to main | Standard; CI on PRs and direct pushes to main. | ✓ (modified) |
| pull_request only | CI on PRs only. | |
| All branches | CI on every branch push. | |

| Option | Description | Selected |
|--------|-------------|----------|
| Node 24 only | Matches engines.node: 24.x. | ✓ |
| Matrix: Node 22 + 24 | Test compatibility with dev machines on Node 22. | |
| node-version: 24.x (floating patch) | Latest 24 patch via setup-node. | |

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — setup-node cache | actions/setup-node with cache: npm. | ✓ |
| No cache | Fresh npm ci every run. | |
| Cache keyed on package-lock.json hash | Explicit cache action. | |

**User's choice:** Parallel jobs, Node 24 only, npm cache via setup-node
**Notes:** Trunk-based development with no PRs for now. CI triggers: push + pull_request on main only (PR trigger reserved for future). User asked to document trunk-based workflow in project artifacts.

---

## Build Step in CI

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — add build job now | Catches compile errors. Phase 5 Docker reuses build step. | ✓ |
| Defer to Phase 5 | test/lint/typecheck only until Docker. | |
| Build as final step in test job | Run build after tests in test job. | |

| Option | Description | Selected |
|--------|-------------|----------|
| No artifact upload | Build verifies compilation only. | ✓ |
| Upload dist/ artifact | Preserve build output for debugging. | |

| Option | Description | Selected |
|--------|-------------|----------|
| Same required status check | All CI jobs must pass. | ✓ (modified) |
| Advisory only | Build failures warn but don't block. | |

| Option | Description | Selected |
|--------|-------------|----------|
| README section + status badge | Document required checks + CI badge. | |
| README instructions only | Text instructions for branch protection. | |
| Document + export ruleset YAML | Reproducible GitHub rulesets. | |

**User's choice:** Include build job now, no artifact upload, build failure fails CI
**Notes:** No branch protection documentation — trunk-based without PRs, merge blocking not applicable. User: "let's not spend time there."

---

## Claude's Discretion

None — user made explicit choices in all areas.

## Deferred Ideas

- Prettier / code formatter — explicitly rejected
- Git pre-commit hooks — rejected
- Branch protection / CI badge documentation — not needed for trunk-based workflow
- dist/ artifact upload — deferred to Phase 5 if needed
- Node 22 compatibility matrix — rejected
- Docker build job — Phase 5
