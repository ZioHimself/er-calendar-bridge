---
phase: 01.1-ci-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - TEST-05
  - OPS-04
user_setup: []

must_haves:
  truths:
    - "D-08: CI has four parallel named jobs — test, lint, typecheck, build"
    - "D-09: Workflow triggers on push and pull_request targeting main only"
    - "D-10: CI uses Node.js 24 only — no version matrix"
    - "D-11: Each job uses npm ci with actions/setup-node cache: npm"
    - "D-12: build job runs npm run build verifying tsc emit"
    - "D-13: No dist/ artifact upload in any job"
    - "D-14: Build failure fails workflow same as other jobs — no branch protection config"
    - "D-01: No Prettier or format:check job in workflow"
    - "D-03: No pre-commit hook setup — CI is the quality gate"
    - "TEST-05: test job runs fixture-only vitest with zero secrets or env vars"
    - "Workflow structured for Phase 5 docker-build job append without restructuring"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "GitHub Actions CI with 4 parallel jobs"
      contains: "jobs:"
      exports: ["test", "lint", "typecheck", "build"]
  key_links:
    - from: .github/workflows/ci.yml
      to: package.json
      via: "npm test, npm run lint, npm run typecheck, npm run build"
      pattern: "npm (test|run lint|run typecheck|run build)"
    - from: .github/workflows/ci.yml
      to: actions/setup-node@v7
      via: "node-version 24 and cache npm"
      pattern: "node-version: 24"
    - from: test/smoke.test.ts
      to: .github/workflows/ci.yml test job
      via: "fixture-only tests need no credentials"
      pattern: "npm test"
---

<objective>
Add greenfield GitHub Actions CI workflow with four parallel jobs (test, lint, typecheck, build) that exercise existing npm scripts on every push and PR to `main`.

Purpose: Satisfy TEST-05 (CI runs tests without live credentials) and OPS-04 (minimum viable CI tooling) while establishing Phase 5 docker-build extension point per D-08.
Output: `.github/workflows/ci.yml` with four named top-level jobs, no secrets, no artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-ci-pipeline/01.1-CONTEXT.md
@.planning/phases/01.1-ci-pipeline/01.1-RESEARCH.md
@.planning/phases/01.1-ci-pipeline/01.1-PATTERNS.md

## Locked decisions implemented in this plan
- D-01, D-02, D-03: No Prettier, no format scripts, no husky — eslint-only CI gate
- D-08: Four parallel named jobs (test, lint, typecheck, build)
- D-09: Triggers on push and pull_request to main
- D-10: Node.js 24 only
- D-11: npm ci with setup-node cache: npm
- D-12, D-13, D-14: Build job verifies emit; no artifact upload; no branch protection
- TEST-05: Fixture-only vitest — no env vars or secrets in workflow

## npm script contract (invoke by name only — no inline vitest/eslint/tsc)

From package.json:
- `"test": "vitest run"`
- `"lint": "eslint ."`
- `"typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json"` (updated in plan 01.1-01)
- `"build": "tsc -p tsconfig.json"`

## Phase 5 extension point (structure only — do NOT implement docker-build now)

Keep four jobs as sibling top-level jobs so Phase 5 can append `docker-build` without restructuring.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow with four parallel jobs</name>
  <read_first>
    .planning/phases/01.1-ci-pipeline/01.1-CONTEXT.md (D-01 through D-14)
    .planning/phases/01.1-ci-pipeline/01.1-RESEARCH.md (Pattern 1, trigger config, security domain)
    .planning/phases/01.1-ci-pipeline/01.1-PATTERNS.md (ci.yml establishment template)
    package.json
    vitest.config.ts
    test/helpers/load-fixture.ts
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create `.github/workflows/ci.yml` as greenfield CI per D-08 through D-14 and RESEARCH Pattern 1:

    Workflow metadata:
    - `name: CI`
    - `on.push.branches: [main]` and `on.pull_request.branches: [main]` per D-09
    - Workflow-level `permissions: contents: read` per RESEARCH Security Domain

    Four parallel top-level jobs — each `runs-on: ubuntu-latest`, each with identical bootstrap then one npm script:
    1. `test` → `npm test` (fixture-only vitest per TEST-05 — no `env:` block, no secrets)
    2. `lint` → `npm run lint`
    3. `typecheck` → `npm run typecheck`
    4. `build` → `npm run build`

    Bootstrap steps in every job (duplicated per job — no YAML anchors or composite actions per OPS-04):
    - `uses: actions/checkout@v7`
    - `uses: actions/setup-node@v7` with `node-version: 24` and `cache: npm` per D-10, D-11
    - `run: npm ci` per D-11

    Explicitly do NOT add:
    - Prettier or `format:check` job (D-01)
    - Node version matrix (D-10)
    - `upload-artifact` or dist/ upload steps (D-13)
    - `secrets:` or provider credential env vars (TEST-05)
    - `docker-build` job (Phase 5 scope)
    - Branch protection configuration (D-14 — not in workflow file)

    Invoke npm scripts by name only — do not inline `vitest`, `eslint`, or `tsc` commands in workflow YAML.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -c 'jobs:' .github/workflows/ci.yml | grep -q '^1$' && grep -E '^\s+(test|lint|typecheck|build):' .github/workflows/ci.yml | wc -l | grep -q '^4$'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists
    - Workflow has exactly four named jobs: test, lint, typecheck, build
    - Triggers include both push and pull_request on main branch
    - All jobs use `actions/checkout@v7` and `actions/setup-node@v7` with `node-version: 24`
    - All jobs run `npm ci` then one npm script
    - Workflow has `permissions: contents: read`
    - No `upload-artifact`, `secrets:`, Prettier, or docker-build references
  </acceptance_criteria>
  <done>D-08 through D-14 and TEST-05 CI structure committed; Phase 5 extension point preserved</done>
</task>

<task type="auto">
  <name>Task 2: Verify workflow contract matches package.json scripts</name>
  <read_first>
    .planning/phases/01.1-ci-pipeline/01.1-VALIDATION.md (per-task verification map)
    .github/workflows/ci.yml
    package.json
    eslint.config.mjs
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Verify CI workflow YAML contract only — do NOT run the full local npm gate here (Plan 01.1-03 Task 2 runs integration gate after Plan 01 typecheck config lands):

    1. Confirm each job's final step maps to an existing package.json script name (`npm test`, `npm run lint`, `npm run typecheck`, `npm run build`).
    2. Confirm no job defines `env:` with provider credentials (mailbox.org, Google, Microsoft, SMTP).
    3. Confirm job count is four siblings at top level — no nested job groups that would block Phase 5 `docker-build` append.
    4. Confirm workflow uses `actions/checkout@v7`, `actions/setup-node@v7` with `node-version: 24`, `cache: npm`, and `npm ci` in every job.

    Fix only `.github/workflows/ci.yml` if structural issues found. Do not add secrets, artifacts, or extra jobs.
  </action>
  <verify>
    <automated>grep -E '^\s+(test|lint|typecheck|build):' .github/workflows/ci.yml | wc -l | grep -q '^4$' && grep -q 'node-version: 24' .github/workflows/ci.yml && grep -q 'cache: npm' .github/workflows/ci.yml && ! grep -E 'secrets:|upload-artifact|prettier|husky|docker-build' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - Workflow YAML contains no `secrets:`, `upload-artifact`, `prettier`, `husky`, or `docker-build`
    - Each CI job final command matches a package.json script name
    - All four jobs use checkout@v7, setup-node@v7 (Node 24, cache npm), and npm ci
    - TEST-05: test job requires no credential env vars
    - OPS-04: only two GitHub Actions used (checkout@v7, setup-node@v7) plus npm ci
  </acceptance_criteria>
  <done>CI workflow verified against local script contract; ready for first push to GitHub</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → npm registry | `npm ci` installs from committed lockfile |
| Fork PR → workflow | pull_request trigger on main only; no secrets configured (D-09) |
| GITHUB_TOKEN | Workflow needs read-only repo access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-03 | Elevation of Privilege | GITHUB_TOKEN | mitigate | `permissions: contents: read` at workflow level |
| T-01.1-04 | Tampering | Third-party actions | mitigate | Pin official `actions/checkout@v7` and `actions/setup-node@v7` only |
| T-01.1-05 | Information Disclosure | Fork PR secret exfiltration | accept | No secrets configured; fixture-only tests (TEST-05) |
| T-01.1-SC | Tampering | npm/pip/cargo installs | mitigate | `npm ci` from committed package-lock.json; no new packages in this phase |
</threat_model>

<verification>
After plan 01.1-02:
- `.github/workflows/ci.yml` has 4 parallel jobs with correct triggers and permissions
- YAML contract verified (full local gate deferred to Plan 01.1-03 Task 2)
- First GitHub push verification is manual post-merge (see 01.1-VALIDATION.md)
</verification>

<success_criteria>
1. Four parallel CI jobs committed per D-08
2. Node 24, npm ci, cache npm per D-10, D-11
3. No secrets, artifacts, Prettier, or husky per D-01, D-03, D-13, TEST-05
4. Workflow structure allows Phase 5 docker-build append
5. OPS-04 minimum viable tooling — two pinned official actions only
</success_criteria>

<output>
Create `.planning/phases/01.1-ci-pipeline/01.1-02-SUMMARY.md` when done
</output>
