---
phase: 03-caldav-read-uid-store-and-google-sync-loop
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - src/adapters/caldav/client.ts
  - src/adapters/caldav/reader.ts
  - src/adapters/caldav/index.ts
  - test/adapters/caldav-readonly.test.ts
  - test/adapters/caldav-reader.test.ts
autonomous: true
requirements:
  - SEC-01
  - SYNC-01
  - SYNC-12
  - SYNC-13
user_setup: []

must_haves:
  truths:
    - "CalDAV adapter exposes only read/sync operations — no source writes (SEC-01)"
    - "Reader returns SyncDelta with changed ICS payloads and deleted tombstones (D-05)"
    - "Delta strategy follows 03-SPIKE-SYNC.md degraded_mode recommendation (D-02)"
    - "mailbox.org ~1-year horizon documented in README note (SYNC-13, D-20)"
  artifacts:
    - path: src/adapters/caldav/reader.ts
      provides: "CalDavReader.poll implementation"
      exports: ["createCalDavReader"]
    - path: src/adapters/caldav/client.ts
      provides: "tsdav DAVClient factory"
    - path: test/adapters/caldav-readonly.test.ts
      provides: "SEC-01 export surface guard"
  key_links:
    - from: src/adapters/caldav/reader.ts
      to: src/adapters/ical/index.js
      via: "parseIcsToSourceEvents per changed object"
      pattern: "parseIcsToSourceEvents"
    - from: src/adapters/caldav/reader.ts
      to: src/store/sync-state-store
      via: "persist sync_token and degraded_mode"
      pattern: "updateSyncState"
---

<objective>
Implement read-only mailbox.org CalDAV client and CalDavReader that produces SyncDelta per spike-selected strategy, routing tombstones for cancel-not-delete sync (D-04/D-05).

Purpose: One-way read path for SYNC-01; deletion discovery for SYNC-12 without CalDAV writes.
Output: `src/adapters/caldav/*`, unit tests for read-only surface and mocked poll behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-SPIKE-SYNC.md
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md (tsdav smartCollectionSync, deleted 404s)
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-PATTERNS.md (caldav adapter boundary)
@src/sync/types.ts
@src/config/env.ts
@src/adapters/ical/parse-source-event.ts

## Locked decisions
- D-01: Single MAILBOX_CALENDAR_URL
- D-02/D-05: sync-collection preferred; snapshot/basic per spike
- D-20: No client-side far-future filter; document horizon in README (Task 3)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read-only CalDAV client factory</name>
  <read_first>
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md (tsdav DAVClient example)
    src/config/env.ts
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-PATTERNS.md (client.ts analog)
  </read_first>
  <files>src/adapters/caldav/client.ts, src/adapters/caldav/index.ts</files>
  <action>
    Implement `createCalDavClient(config: Pick<AppConfig, 'mailboxCalDavUrl' | 'mailboxUsername' | 'mailboxAppPassword'>)` using tsdav DAVClient with Basic auth and `await client.login()`.

    Export only read-oriented helpers from `index.ts` — re-export client factory and reader factory; do not re-export tsdav write helpers (createCalendarObject, updateCalendarObject, deleteCalendarObject).

    Map login failures to Error messages without password substring.

    Per SEC-01: add file-level comment that this module must never call tsdav mutation APIs.
  </action>
  <verify>
    <automated>npm run typecheck && test -f src/adapters/caldav/client.ts</automated>
  </verify>
  <acceptance_criteria>
    - client.ts is only module importing tsdav
    - index.ts exports createCalDavClient and createCalDavReader only
  </acceptance_criteria>
  <done>CalDAV client factory ready</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CalDavReader.poll with spike-driven delta strategy</name>
  <read_first>
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-SPIKE-SYNC.md
    src/store/sync-state-store.ts
    src/sync/types.ts
    scripts/spike-mailbox-sync.mts
  </read_first>
  <files>src/adapters/caldav/reader.ts, test/adapters/caldav-reader.test.ts</files>
  <behavior>
    - When tsdav returns changed calendar objects, poll maps them to changed[] with href and calendar-data string
    - When tsdav reports deleted/404 hrefs, poll maps to deleted[] entries
    - After poll, sync_token/ctag persisted via sync state store for configured calendar URL
    - degraded_mode from spike written to sync_state on first poll
  </behavior>
  <action>
    Implement `createCalDavReader({ client, calendarUrl, syncStateStore, mappingStore })` implementing `CalDavReader`.

    Branch implementation per 03-SPIKE-SYNC.md `degraded_mode`:
    - `webdav_sync`: use smartCollectionSync/webdav path; persist sync_token
    - `basic_sync` or `snapshot_diff`: use tsdav basic sync or href/uid snapshot diff per RESEARCH; map absent uids to deleted via mapping store href/uid snapshot when needed (D-05)

    For deleted entries without uid, resolve uid from mapping store by href if snapshot table populated; else pass href-only for orchestrator to skip with warn log.

    Add `test/adapters/caldav-reader.test.ts` with vi.mock on tsdav client methods returning fixture ICS from test/fixtures and deleted href list — no network.

    Commit tests first if extending TDD: RED then GREEN for reader mapping logic.
  </action>
  <verify>
    <automated>npm test -- test/adapters/caldav-reader.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Reader tests pass without live CalDAV
    - poll() returns SyncDelta matching port types from 03-02
    - Sync token and degraded_mode persisted across mocked polls
  </acceptance_criteria>
  <done>CalDavReader implements delta poll per spike</done>
</task>

<task type="auto">
  <name>Task 3: SEC-01 read-only export guard and SYNC-13 ops note</name>
  <read_first>
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-CONTEXT.md (D-20)
    src/adapters/caldav/index.ts
    README.md
  </read_first>
  <files>test/adapters/caldav-readonly.test.ts, README.md</files>
  <action>
    Create `test/adapters/caldav-readonly.test.ts` asserting exported names from `src/adapters/caldav/index.ts` do not include write-oriented symbols (static list or grep-based test reading index source).

    Add README section "CalDAV horizon (SYNC-13)" documenting mailbox.org ~1-year future event limit and that bridge syncs what CalDAV returns (D-20) — short paragraph, link to mailbox.org forum if already cited in RESEARCH.

    Do not add live integration tests.
  </action>
  <verify>
    <automated>npm test -- test/adapters/caldav-readonly.test.ts && grep -q 'SYNC-13\|1-year\|horizon' README.md</automated>
  </verify>
  <acceptance_criteria>
    - caldav-readonly.test.ts passes
    - README documents CalDAV horizon for operators
  </acceptance_criteria>
  <done>SEC-01 guard and SYNC-13 documentation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bridge → mailbox.org CalDAV | Credentials; read-only contract SEC-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | caldav adapter | mitigate | No write API exports; caldav-readonly.test.ts |
| T-03-02 | Information disclosure | reader logs | mitigate | Do not log full ICS at info level; redact credentials |
| T-03-07 | Denial of service | large calendar fetch | mitigate | Delta path per spike; log degraded_mode when full fetch |
</threat_model>

<verification>
npm test -- test/adapters/ && npm run typecheck
</verification>

<success_criteria>
Read-only CalDAV reader produces SyncDelta and persists sync metadata; SYNC-13 documented.
</success_criteria>

<output>
Create `.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-04-SUMMARY.md` when done
</output>
