---
phase: 03-caldav-read-uid-store-and-google-sync-loop
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/spike-mailbox-sync.mts
  - .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-SPIKE-SYNC.md
autonomous: false
requirements:
  - SYNC-01
user_setup:
  - service: mailbox.org CalDAV
    why: "Wave 0 spike validates sync-collection vs basic delta (D-02)"
    env_vars:
      - name: MAILBOX_CALDAV_URL
        source: "mailbox.org settings — typically https://dav.mailbox.org"
      - name: MAILBOX_CALENDAR_URL
        source: "Per-calendar CalDAV URL from mailbox.org calendar properties (D-01)"
      - name: MAILBOX_USERNAME
        source: "Operator mailbox.org account"
      - name: MAILBOX_APP_PASSWORD
        source: "mailbox.org → Security → App passwords (read-only CalDAV)"
must_haves:
  truths:
    - "Operator knows whether mailbox.org supports sync-collection or requires basic/snapshot fallback (D-02)"
    - "Runtime dependencies for Phase 3 are installed and lockfile committed"
    - "Spike outcome recorded with degraded_mode recommendation for reader implementation"
  artifacts:
    - path: .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-SPIKE-SYNC.md
      provides: "Documented PROPFIND/sync-collection trial results"
      contains: "degraded_mode"
    - path: scripts/spike-mailbox-sync.mts
      provides: "Runnable spike using tsdav against operator calendar"
    - path: package-lock.json
      provides: "Pinned tsdav, googleapis, better-sqlite3, zod, pino"
  key_links:
    - from: scripts/spike-mailbox-sync.mts
      to: tsdav DAVClient
      via: "login + smartCollectionSync trial"
      pattern: "DAVClient"
---

<objective>
Validate mailbox.org CalDAV delta semantics (sync-collection vs basic/CTag fallback per D-02), install Phase 3 runtime dependencies after package legitimacy review, and record spike outcomes for CalDAV reader implementation.

Purpose: STATE blocker — do not lock delta strategy without provider evidence; unblock parallel env/store work in Wave 1.
Output: `03-SPIKE-SYNC.md`, `scripts/spike-mailbox-sync.mts`, updated `package.json` / lockfile with audited packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-CONTEXT.md
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-PATTERNS.md
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-VALIDATION.md
@.planning/research/ARCHITECTURE.md

## Locked decisions (this plan)
- D-01: Single `MAILBOX_CALENDAR_URL` in spike (not multi-calendar)
- D-02: Prefer sync-collection; document fallback if 403/unsupported
- D-05: Note whether removed UIDs/hrefs appear in delta for tombstone routing

## Package Legitimacy (RESEARCH audit — all [ASSUMED])
tsdav, googleapis, google-auth-library, better-sqlite3, zod, pino — human verifies on npm before install.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify npm package legitimacy before install</name>
  <read_first>
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md (Package Legitimacy Audit table)
  </read_first>
  <files>.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md</files>
  <action>
    Pause for human package review before any npm install. Present the six [ASSUMED] packages from RESEARCH (tsdav, googleapis, google-auth-library, better-sqlite3, zod, pino) and the verification steps in how-to-verify. Do not run npm install until checkpoint resumes with approval.
  </action>
  <what-built>Checklist of six Phase 3 packages tagged [ASSUMED] pending slopcheck</what-built>
  <how-to-verify>
    1. Open each package on npm: tsdav, googleapis, google-auth-library, better-sqlite3, zod, pino
    2. Confirm publisher matches RESEARCH source repos (e.g. natelindev/tsdav, WiseLibs/better-sqlite3, colinhacks/zod)
    3. Reject any package with suspicious postinstall scripts or typosquat names
    4. Reply "approved" to proceed with `npm install` in Task 2
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md && grep -q 'Package Legitimacy Audit' .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md</automated>
  </verify>
  <acceptance_criteria>
    - Human explicitly approved all six packages or flagged a substitute before install
    - No [SLOP] packages installed
  </acceptance_criteria>
  <done>Human approved npm package set for Task 2 install</done>
  <resume-signal>Type "approved" after review, or name packages to exclude</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install Phase 3 dependencies and add spike script scaffold</name>
  <read_first>
    package.json
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md (Installation + tsdav spike steps)
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-PATTERNS.md (package.json scripts)
  </read_first>
  <files>package.json, package-lock.json, scripts/spike-mailbox-sync.mts</files>
  <action>
    After Task 1 approval, run: npm install tsdav googleapis google-auth-library better-sqlite3 zod pino && npm install -D @types/better-sqlite3

    Add npm script `spike:mailbox-sync` running `tsx scripts/spike-mailbox-sync.mts` (do not add to CI).

    Create `scripts/spike-mailbox-sync.mts` that:
    - Loads env via minimal inline Zod or process.env reads (full `loadConfig` comes in 03-02) for MAILBOX_CALDAV_URL, MAILBOX_CALENDAR_URL, MAILBOX_USERNAME, MAILBOX_APP_PASSWORD
    - Constructs tsdav DAVClient with Basic auth per RESEARCH example
    - Logs supported-report-set / whether syncCollection is advertised
    - Attempts one smartCollectionSync or syncCollection trial on MAILBOX_CALENDAR_URL
    - Prints HTTP status, whether sync-token updated, count of changed vs deleted (404) entries
    - Exits 0 on success, non-zero on auth/config failure; never calls tsdav write APIs (SEC-01)

    Create `.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-SPIKE-SYNC.md` template with sections: Date, Calendar URL, sync-collection supported (Y/N), HTTP codes, recommended `degraded_mode` (`webdav_sync` | `basic_sync` | `snapshot_diff`), tombstone discovery notes, payload size observation (SYNC-13 horizon).

    Run `npm run typecheck` after script compiles.
  </action>
  <verify>
    <automated>npm run typecheck && node -e "const p=require('./package.json'); ['tsdav','googleapis','better-sqlite3','zod','pino'].forEach(d=>{if(!p.dependencies[d]) process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - All six runtime deps and @types/better-sqlite3 present in package.json
    - Spike script exists and typechecks; no CalDAV create/update/delete calls
    - 03-SPIKE-SYNC.md template committed with degraded_mode section
  </acceptance_criteria>
  <done>Dependencies installed; spike tooling ready for operator run</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run mailbox.org spike and record outcome</name>
  <read_first>
    scripts/spike-mailbox-sync.mts
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-SPIKE-SYNC.md
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-CONTEXT.md (D-02, D-05)
  </read_first>
  <files>.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-SPIKE-SYNC.md</files>
  <action>
    After Task 2, pause for operator to run the spike against live mailbox.org credentials and commit filled results into 03-SPIKE-SYNC.md. Executor may assist editing the doc from operator-reported output but must not embed real passwords in the repository.
  </action>
  <what-built>Spike script and outcome template from Task 2</what-built>
  <how-to-verify>
    1. Copy `.env.example` to `.env` and fill MAILBOX_* vars with operator read-only app password
    2. Set `MAILBOX_CALENDAR_URL` to the per-calendar collection URL (not server root only)
    3. Run `npm run spike:mailbox-sync`
    4. Fill `03-SPIKE-SYNC.md` with observed support for sync-collection, token behavior, deleted href handling
    5. Confirm recommended `degraded_mode` for plan 03-04 implementer
    6. Reply "approved" when doc is complete
  </how-to-verify>
  <verify>
    <automated>grep -q 'degraded_mode' .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-SPIKE-SYNC.md && ! grep -q 'TEMPLATE:' .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-SPIKE-SYNC.md</automated>
  </verify>
  <acceptance_criteria>
    - 03-SPIKE-SYNC.md contains filled results (not template placeholders)
    - degraded_mode recommendation is explicit for CalDAV reader
    - Spike used read-only CalDAV operations only
  </acceptance_criteria>
  <done>03-SPIKE-SYNC.md completed with provider delta strategy</done>
  <resume-signal>Type "approved" when spike doc is complete</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator host → mailbox.org | CalDAV credentials cross network; TLS required |
| npm registry → project | Supply-chain risk on new dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | npm installs | mitigate | Blocking human checkpoint Task 1; RESEARCH legitimacy table |
| T-03-02 | Information disclosure | spike script logs | mitigate | Redact MAILBOX_APP_PASSWORD in logs; never print full ICS bodies at debug in committed script |
| T-03-03 | Tampering | CalDAV write to source | mitigate | Spike script imports only read/sync APIs; SEC-01 review in 03-04 |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck not run — blocking human checkpoint for [ASSUMED] packages |
</threat_model>

<verification>
- `npm run typecheck` passes after Task 2
- 03-SPIKE-SYNC.md completed after Task 3
</verification>

<success_criteria>
Spike outcome documented; Phase 3 npm dependencies installed; CalDAV reader plan can branch on `degraded_mode`.
</success_criteria>

<output>
Create `.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-01-SUMMARY.md` when done
</output>
