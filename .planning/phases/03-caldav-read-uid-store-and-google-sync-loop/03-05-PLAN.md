---
phase: 03-caldav-read-uid-store-and-google-sync-loop
plan: 05
type: tdd
wave: 2
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/writers/google/auth.ts
  - src/writers/google/calendar-writer.ts
  - test/writers/google-auth.test.ts
  - test/writers/calendar-writer.test.ts
autonomous: true
requirements:
  - SEC-03
  - SYNC-05
  - SYNC-07
user_setup:
  - service: Google Calendar API
    why: "Pilot OAuth refresh token for manual sync runs (not CI)"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console OAuth client"
      - name: GOOGLE_CLIENT_SECRET
        source: "Same OAuth client"
      - name: GOOGLE_REFRESH_TOKEN
        source: "Offline OAuth flow with calendar scope only (SEC-03)"
      - name: GOOGLE_CALENDAR_ID
        source: "Target pilot calendar id"

must_haves:
  truths:
    - "OAuth client uses calendar scope only (SEC-03, D-11)"
    - "Google writes set iCalUID to source uid on create (D-14)"
    - "Busy events use summary Busy and opaque transparency (D-15)"
    - "Cancel uses status cancelled — never events.delete (D-04)"
    - "Recurrence instances use master + instance update/cancel pattern (D-13, SYNC-07)"
  artifacts:
    - path: src/writers/google/calendar-writer.ts
      provides: "CalendarWriter implementation"
      exports: ["createGoogleCalendarWriter"]
    - path: src/writers/google/auth.ts
      provides: "OAuth2 client + scope constant"
      exports: ["GOOGLE_CALENDAR_SCOPE", "createGoogleAuthClient"]
    - path: test/writers/google-auth.test.ts
      provides: "Scope assertion SEC-03"
  key_links:
    - from: src/writers/google/calendar-writer.ts
      to: googleapis calendar.events
      via: "insert/patch with iCalUID and extendedProperties"
      pattern: "events\\.(insert|patch)"
    - from: src/writers/google/calendar-writer.ts
      to: src/domain/wash/wash-source-event.ts
      via: "OutboundEvent field mapping"
      pattern: "Busy"
---

<objective>
Implement Google Calendar API writer with calendar-only OAuth, cancel-not-delete semantics, and recurrence-aware upsert/cancel (SYNC-05, SYNC-07, SEC-03).

Purpose: Target write path for one-way pilot sync; enforces washing rules on Google payload (D-14, D-15).
Output: `src/writers/google/*`, unit tests with mocked googleapis (no live API in CI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md (Google recurrence table, OAuth)
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-PATTERNS.md (calendar-writer analog)
@src/sync/types.ts
@src/domain/wash/wash-source-event.ts
@src/domain/types/index.ts

## Locked decisions
- D-04, D-09, D-10: cancel not delete; update same Google event on tier change
- D-12: GOOGLE_CALENDAR_ID from config
- D-14: iCalUID + private extended property for bridge_uuid (key discretion e.g. er.bridge_uuid)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Google auth scope and writer mapping tests</name>
  <read_first>
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md (OAuth + cancel instance)
    src/domain/wash/wash-source-event.ts
    test/domain/classify.test.ts
  </read_first>
  <files>test/writers/google-auth.test.ts, test/writers/calendar-writer.test.ts</files>
  <behavior>
    - GOOGLE_CALENDAR_SCOPE equals https://www.googleapis.com/auth/calendar or documented narrower calendar.events constant
    - upsertOutbound for busy OutboundEvent calls insert/patch with summary Busy and transparency opaque
    - upsertOutbound sets iCalUID to source uid on create
    - cancel calls patch with status cancelled, not delete
    - upsertOutbound for recurrenceId set includes instance-specific handling flag isRecurringInstance
  </behavior>
  <action>
    Create `test/writers/google-auth.test.ts` importing scope constant from auth module — expect failure until Task 2.

    Create `test/writers/calendar-writer.test.ts` using vi.mock on `googleapis` calendar.events.insert/patch/delete — assert delete never called; mock resolved ids.

    Use minimal OutboundEvent literals from domain types. Commit RED.
  </action>
  <verify>
    <automated>npm test -- test/writers/; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Writer tests mock googleapis; no network
    - Tests encode busy mapping, iCalUID, cancel-not-delete, recurrence instance path
  </acceptance_criteria>
  <done>RED: Google writer and auth tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Google auth and CalendarWriter implementation</name>
  <read_first>
    test/writers/calendar-writer.test.ts
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-CONTEXT.md (D-11 through D-15)
    src/config/env.ts
  </read_first>
  <files>src/writers/google/auth.ts, src/writers/google/calendar-writer.ts</files>
  <action>
    Implement `src/writers/google/auth.ts`:
    - Export `GOOGLE_CALENDAR_SCOPE` (calendar or calendar.events per RESEARCH A3)
    - `createGoogleAuthClient(config)` returning OAuth2Client with refresh token from config; no scope mutation at runtime beyond constant documentation

    Implement `createGoogleCalendarWriter({ auth, calendarId })` satisfying `CalendarWriter`:
    - Map OutboundEvent to Google requestBody; busy: summary Busy, transparency opaque, strip categories/PII fields
    - create: set iCalUID, extendedProperties.private bridge uuid key
    - update: patch existing googleEventId when provided (D-10 tier changes)
    - cancel: patch status cancelled; pass isRecurringInstance to choose instance vs master id (D-13)
    - Return googleEventId from upsert; store googleRecurringEventId on master rows when applicable

    All googleapis imports only in writers/google/. Run tests until green.
  </action>
  <verify>
    <automated>npm test -- test/writers/ && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - google-auth and calendar-writer tests pass
    - events.delete not invoked in implementation or tests
    - SEC-03 scope exported and tested
  </acceptance_criteria>
  <done>GREEN: Google CalendarWriter with cancel-not-delete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bridge → Google Calendar API | OAuth tokens; outbound event content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Elevation | OAuth scope | mitigate | GOOGLE_CALENDAR_SCOPE constant + unit test SEC-03 |
| T-03-08 | Information disclosure | busy-block leak | mitigate | Writer enforces Busy/opaque; full fields only for public outbound |
| T-03-02 | Information disclosure | auth logs | mitigate | pino redact refresh token in CLI wiring (03-06) |
</threat_model>

<verification>
npm test -- test/writers/
</verification>

<success_criteria>
Mock-tested Google writer honors washing, recurrence, and cancel semantics.
</success_criteria>

<output>
Create `.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-05-SUMMARY.md` when done
</output>
