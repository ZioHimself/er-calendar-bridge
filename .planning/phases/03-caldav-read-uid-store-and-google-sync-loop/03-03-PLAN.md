---
phase: 03-caldav-read-uid-store-and-google-sync-loop
plan: 03
type: tdd
wave: 1
depends_on:
  - 03-01
files_modified:
  - src/store/schema.sql
  - src/store/mapping-store.ts
  - src/store/sync-state-store.ts
  - test/store/mapping-store.test.ts
autonomous: true
requirements:
  - SYNC-11
  - OPS-03
user_setup: []

must_haves:
  truths:
    - "Composite key source uid + recurrenceId prevents duplicate mapping rows (D-06)"
    - "Cancelled mappings retained in SQLite with status cancelled (D-09)"
    - "sync_state stores sync_token, last_success_at, degraded_mode for OPS-03"
    - "bridge_uuid generated on first insert (D-07)"
    - "CalDAV href persisted and resolvable to source uid for tombstones (D-05)"
  artifacts:
    - path: src/store/mapping-store.ts
      provides: "getMapping, upsertMapping, markCancelled"
      exports: ["openMappingStore"]
    - path: src/store/sync-state-store.ts
      provides: "sync token and last_success_at persistence"
    - path: src/store/schema.sql
      provides: "event_mappings and sync_state DDL"
    - path: test/store/mapping-store.test.ts
      provides: "Composite key and cancel retention tests"
  key_links:
    - from: src/store/mapping-store.ts
      to: src/store/schema.sql
      via: "DDL on open"
      pattern: "event_mappings"
    - from: src/store/sync-state-store.ts
      to: sync_state table
      via: "calendar_url primary key"
      pattern: "last_success_at"
---

<objective>
Implement SQLite event mapping and sync metadata stores (better-sqlite3) with composite keys and cancel-not-delete row retention per D-06–D-09.

Purpose: Idempotent Google writes (SYNC-11) and operator visibility into last successful sync (OPS-03 partial).
Output: Store modules, schema DDL, passing `test/store/mapping-store.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md (SQLite schema)
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-PATTERNS.md (mapping-store section)
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-CONTEXT.md (D-06 through D-09, D-08)

Use `:memory:` or temp file paths in tests; production path from loadConfig in later plans.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Failing mapping store unit tests</name>
  <read_first>
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md (event_mappings table)
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-PATTERNS.md (mapping-store API)
    test/domain/classify.test.ts
  </read_first>
  <files>test/store/mapping-store.test.ts</files>
  <behavior>
    - Upsert master (recurrenceId undefined) then lookup returns google_event_id and bridge_uuid
    - Second upsert same uid+recurrenceId updates google_event_id without duplicate row (SYNC-11)
    - Master and exception with same uid but different recurrenceId are distinct rows (D-06)
    - markCancelled sets status cancelled but row still exists (D-09)
    - recurrenceId stored as normalized ISO UTC string when Date provided
    - upsertHrefSnapshot({ href, sourceUid, etag? }) then resolveUidByHref(href) returns uid (D-05 tombstone routing)
  </behavior>
  <action>
    Create `test/store/mapping-store.test.ts` importing `openMappingStore` from `../../src/store/mapping-store.js`. Use `:memory:` database path helper.

    Encode behaviors above as dedicated tests. Expect failures until Task 2.

    Commit RED: `test(03-03): add failing mapping store tests`.
  </action>
  <verify>
    <automated>npm test -- test/store/mapping-store.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover composite key, idempotent upsert, cancel retention, master vs instance rows
    - No googleapis or tsdav imports in test file
  </acceptance_criteria>
  <done>RED: mapping store test suite</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Schema, mapping store, and sync state store</name>
  <read_first>
    test/store/mapping-store.test.ts
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md (sync_state, source_href_snapshot or caldav_href)
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-CONTEXT.md (D-05 snapshot fallback)
  </read_first>
  <files>src/store/schema.sql, src/store/mapping-store.ts, src/store/sync-state-store.ts</files>
  <action>
    Add `src/store/schema.sql` with tables `event_mappings` (columns per RESEARCH) and `sync_state` (calendar_url PK, sync_token, ctag, last_success_at, last_error, degraded_mode). Use PRIMARY KEY on (source_uid, recurrence_id) with documented NULL handling via COALESCE(recurrence_id,'') in application layer or composite unique index — pick one approach and document in mapping-store header comment.

    **Required (D-05):** Persist CalDAV object href→source uid for tombstone routing — either `source_href_snapshot` table (href PK, source_uid, etag) per RESEARCH **or** `caldav_href` column on `event_mappings` updated on upsert. Export `upsertHrefSnapshot({ href, sourceUid, etag? })` and `resolveUidByHref(href): string | undefined` (implementation wraps chosen schema).

    Implement `openMappingStore(sqlitePath: string)`:
    - mkdir parent dir if missing (D-08)
    - run schema migrations on open
    - Methods: getMapping({uid, recurrenceId?}), upsertMapping({ uid, recurrenceId?, googleEventId, googleRecurringEventId?, bridgeUuid?, status, lastSourceEtag?, caldavHref? }) — generate bridge_uuid v4 on first insert (D-07)
    - markCancelled({ uid, recurrenceId? })
    - Use db.transaction for upserts

    Implement `openSyncStateStore` sharing same DB file or composed in mapping-store factory — export functions: getSyncState(calendarUrl), updateSyncState({ calendarUrl, syncToken?, ctag?, lastSuccessAt?, lastError?, degradedMode? }), recordSuccess(calendarUrl, isoTimestamp).

    Run tests until green. `npm run typecheck`.
  </action>
  <verify>
    <automated>npm test -- test/store/mapping-store.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All mapping-store tests pass
    - sync_state helpers persist last_success_at for OPS-03
    - better-sqlite3 confined to src/store/
  </acceptance_criteria>
  <done>GREEN: SQLite mapping and sync state stores</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host filesystem → SQLite file | Local tampering of mappings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | SQLite file | accept | Pilot on operator laptop; file permissions; Docker volume in Phase 5 |
| T-03-06 | Information disclosure | mapping rows | accept | Stores Google event ids only; no ICS PII required in mapping table |
</threat_model>

<verification>
npm test -- test/store/mapping-store.test.ts
</verification>

<success_criteria>
Composite-key SQLite store supports idempotent sync and sync metadata for orchestrator.
</success_criteria>

<output>
Create `.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-03-SUMMARY.md` when done
</output>
