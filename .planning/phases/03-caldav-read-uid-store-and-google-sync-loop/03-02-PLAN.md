---
phase: 03-caldav-read-uid-store-and-google-sync-loop
plan: 02
type: tdd
wave: 1
depends_on:
  - 03-01
files_modified:
  - src/sync/types.ts
  - src/config/env.ts
  - .env.example
  - test/config/env.test.ts
autonomous: true
requirements:
  - SEC-04
  - OPS-01
user_setup: []

must_haves:
  truths:
    - "Process fails fast at startup when required pilot env vars missing (D-03)"
    - "D-08: SQLite database path configurable via SQLITE_PATH or DATA_DIR with default ./data/bridge.db"
    - "Single CalDAV calendar URL and single Google calendar ID enforced (D-01, D-12, OPS-01)"
    - "Sync ports defined for mocked integration tests (D-19)"
    - "SYNC_INTERVAL_SECONDS default 300 replaces minutes in .env.example (D-16)"
  artifacts:
    - path: src/config/env.ts
      provides: "loadConfig() readonly typed config"
      exports: ["loadConfig"]
    - path: src/sync/types.ts
      provides: "CalDavReader, CalendarWriter, SyncDelta, SyncCycleDeps"
      exports: ["SyncDelta", "CalDavReader", "CalendarWriter"]
    - path: test/config/env.test.ts
      provides: "Zod env unit tests"
  key_links:
    - from: src/config/env.ts
      to: process.env
      via: "zod safeParse once at boot"
      pattern: "safeParse"
    - from: src/sync/types.ts
      to: src/domain/types/index.ts
      via: "import type OutboundEvent"
      pattern: "OutboundEvent"
---

<objective>
Define sync port contracts and Zod-validated single-account configuration (SEC-04, OPS-01) using TDD, aligning `.env.example` with locked env names and seconds-based sync interval.

Purpose: Fail-closed config before any network adapters; ports unblock CalDAV reader, Google writer, and orchestrator tests.
Output: `src/sync/types.ts`, `src/config/env.ts`, `.env.example`, passing `test/config/env.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-CONTEXT.md
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md (Pattern 5 env table, CalendarWriter interface)
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-PATTERNS.md (env.ts, sync/types)
@.env.example

## Locked decisions
- D-03: Zod-validated env for pilot credentials
- D-08: SQLITE_PATH or DATA_DIR with default under ./data/
- D-16: SYNC_INTERVAL_SECONDS default 300

## Interfaces (domain — unchanged)

From `src/domain/types/index.ts`: `SourceEvent`, `OutboundEvent`, `ProcessedSourceEvent`, `PropagationDecision`.

## CalendarWriter shape (implement in 03-05)

Per RESEARCH: upsertOutbound with mappingKey `{ uid, recurrenceId? }`, cancel with googleEventId, cancel-not-delete semantics (D-04).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Port types and failing env schema tests</name>
  <read_first>
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md (CalendarWriter, SyncDelta, env table)
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-PATTERNS.md (src/sync/types.ts section)
    src/domain/types/index.ts
    test/domain/classify.test.ts
  </read_first>
  <files>src/sync/types.ts, test/config/env.test.ts</files>
  <behavior>
    - Missing MAILBOX_CALENDAR_URL → loadConfig throws or returns error with field name
    - Missing GOOGLE_CALENDAR_ID → fail closed
    - Default SYNC_INTERVAL_SECONDS is 300 when unset
    - Strict schema rejects unknown env keys when using z.strictObject or equivalent SEC-04 pattern
    - SQLITE_PATH defaults to ./data/bridge.db when DATA_DIR unset (D-08)
    - Parsed config exposes mailserverUrl, calendarUrl, googleCalendarId as readonly strings
  </behavior>
  <action>
    Create `src/sync/types.ts` exporting:
    - `SyncDelta` with `changed: Array<{ href: string; data: string; etag?: string }>` and `deleted: Array<{ href: string; uid?: string; recurrenceId?: string }>`
    - `CalDavReader` with `poll(): Promise<SyncDelta>`
    - `CalendarWriter` with `upsertOutbound` and `cancel` per RESEARCH (use `import type` for `OutboundEvent`)
    - `SyncCycleDeps`, `SyncCycleResult` (counts: created, updated, cancelled, dropped, errors; optional lastSuccessAt)
    - `Logger` minimal interface for pino-compatible `{ info, warn, error }`

    Create `test/config/env.test.ts` with `minimalEnv(overrides)` helper pattern from PATTERNS. Import `loadConfig` from `../../src/config/env.js` — expect failure until Task 2.

    Do not read `process.env` outside future env module. Commit RED: `test(03-02): add failing env schema tests`.
  </action>
  <verify>
    <automated>npm test -- test/config/env.test.ts; test $? -ne 0; test -f src/sync/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - Port types exported from src/sync/types.ts with no tsdav/googleapis imports
    - env.test.ts encodes required vars, defaults, and strict unknown-key rejection
    - Tests fail before loadConfig implementation
  </acceptance_criteria>
  <done>RED: port contracts + failing env tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement loadConfig and update .env.example</name>
  <read_first>
    test/config/env.test.ts
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-CONTEXT.md (D-01, D-03, D-08, D-11, D-12, D-16)
    .env.example
  </read_first>
  <files>src/config/env.ts, .env.example</files>
  <action>
    Implement `src/config/env.ts`:
    - Zod schema for MAILBOX_CALDAV_URL, MAILBOX_CALENDAR_URL, MAILBOX_USERNAME, MAILBOX_APP_PASSWORD, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_REFRESH_TOKEN, GOOGLE_CALENDAR_ID, optional SQLITE_PATH, DATA_DIR, SYNC_INTERVAL_SECONDS (coerce number, default 300), LOG_LEVEL (default info)
    - `export function loadConfig(env: NodeJS.ProcessEnv = process.env): AppConfig` — on failure throw Error with flattened field errors; never echo password or refresh token values in message (redact)
    - Resolve sqlite path: SQLITE_PATH ?? join(DATA_DIR ?? './data', 'bridge.db')
    - Export readonly `AppConfig` type

    Update `.env.example`: add MAILBOX_CALENDAR_URL, GOOGLE_CALENDAR_ID, SQLITE_PATH or DATA_DIR comment, replace SYNC_INTERVAL_MINUTES with SYNC_INTERVAL_SECONDS=300; note read-only CalDAV (SEC-01). Keep Microsoft/SMTP placeholders unchanged for later phases.

    Run tests until green. Commit: `feat(03-02): Zod env loader and sync port types`.
  </action>
  <verify>
    <automated>npm test -- test/config/env.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All env.test.ts cases pass
    - .env.example documents seconds interval and pilot calendar URLs
    - No secrets logged from loadConfig error paths
  </acceptance_criteria>
  <done>GREEN: loadConfig and .env.example aligned with D-03/D-16</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env → loadConfig | Untrusted shape; must validate before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information disclosure | loadConfig errors | mitigate | Redact MAILBOX_APP_PASSWORD and GOOGLE_REFRESH_TOKEN in error strings |
| T-03-04 | Elevation | SEC-04 multi-tenant | mitigate | Single-account schema; strict object rejects extra keys |
| T-03-05 | Spoofing | Missing calendar URL | mitigate | Required MAILBOX_CALENDAR_URL and GOOGLE_CALENDAR_ID |
</threat_model>

<verification>
npm test -- test/config/env.test.ts && npm run typecheck
</verification>

<success_criteria>
loadConfig validates pilot env; sync port types ready for adapters and orchestrator.
</success_criteria>

<output>
Create `.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-02-SUMMARY.md` when done
</output>
