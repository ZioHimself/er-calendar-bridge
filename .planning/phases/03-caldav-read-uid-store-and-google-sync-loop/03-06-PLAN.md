---
phase: 03-caldav-read-uid-store-and-google-sync-loop
plan: 06
type: tdd
wave: 3
depends_on:
  - 03-02
  - 03-03
  - 03-04
  - 03-05
files_modified:
  - src/sync/run-sync-cycle.ts
  - src/index.ts
  - test/integration/sync-cycle.test.ts
  - test/integration/sync-recurrence.test.ts
  - README.md
autonomous: true
requirements:
  - SYNC-01
  - SYNC-05
  - SYNC-06
  - SYNC-07
  - SYNC-11
  - SYNC-12
  - OPS-03
user_setup: []

must_haves:
  truths:
    - "runSyncCycle wires CalDAV → parseIcsToSourceEvents → processSourceEvent → store → writer (SYNC-01)"
    - "Drop/sensitive cancels existing Google mapping (SYNC-05)"
    - "Second cycle does not duplicate upserts (SYNC-11)"
    - "Source delete delta cancels Google event (SYNC-12, D-04)"
    - "Recurrence fixtures drive instance behavior (SYNC-07)"
    - "CLI sync and sync --watch run one cycle or loop on SYNC_INTERVAL_SECONDS (D-17, SYNC-06)"
    - "Changed ICS same uid on later cycle updates in place — no duplicate create (SYNC-06, D-10)"
    - "Tier change public ↔ busy updates same mapped Google event in place (D-10)"
    - "Cycle logs counts and persists last_success_at (OPS-03, D-18)"
  artifacts:
    - path: src/sync/run-sync-cycle.ts
      provides: "runSyncCycle(deps)"
      exports: ["runSyncCycle"]
    - path: src/index.ts
      provides: "CLI sync subcommand"
    - path: test/integration/sync-cycle.test.ts
      provides: "Mocked E2E acceptance (D-19)"
    - path: test/integration/sync-recurrence.test.ts
      provides: "Recurrence integration tests"
  key_links:
    - from: src/sync/run-sync-cycle.ts
      to: src/domain/process-source-event.js
      via: "per SourceEvent"
      pattern: "processSourceEvent"
    - from: src/index.ts
      to: src/sync/run-sync-cycle.js
      via: "wired real adapters"
      pattern: "runSyncCycle"
    - from: src/sync/run-sync-cycle.ts
      to: src/adapters/ical/index.js
      via: "parseIcsToSourceEvents"
      pattern: "parseIcsToSourceEvents"
---

<objective>
Wire port-based sync orchestration, mocked integration acceptance tests, and CLI entrypoints (`sync`, `sync --watch`) completing the Google pilot loop.

Purpose: Deliver end-to-end one-way sync behavior in CI without live credentials (D-19) and operator-runnable CLI (OPS-01, OPS-03).
Output: `run-sync-cycle.ts`, integration tests, production CLI wiring in `index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-CONTEXT.md (D-17, D-18, D-19)
@.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-VALIDATION.md
@src/domain/process-source-event.ts
@test/helpers/load-fixture.ts
@src/index.ts

## Pipeline (unchanged)
parseIcsToSourceEvents → processSourceEvent per event → writer/store decisions.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Integration tests for sync cycle and recurrence</name>
  <read_first>
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-RESEARCH.md (mocked integration example)
    test/domain/pipeline-fixtures.test.ts
    test/helpers/load-fixture.ts
    src/sync/types.ts
  </read_first>
  <files>test/integration/sync-cycle.test.ts, test/integration/sync-recurrence.test.ts</files>
  <behavior>
    - Two runSyncCycle calls with same changed fixture → upsertOutbound called once (SYNC-11, idempotent)
    - Second cycle with changed ICS same uid but different summary/start → upsertOutbound called again with existingGoogleEventId from store, not a second insert (SYNC-06 update propagation)
    - Same mapped event: tier public then busy (or busy then public) across cycles → upsertOutbound updates same google_event_id in place, no cancel+recreate (D-10)
    - propagation drop with existing mapping → cancel called, no upsert (SYNC-05)
    - deleted delta with mapped uid → cancel called (SYNC-12)
    - cycle result includes counts and store records last_success_at (OPS-03)
    - modified-instance and deleted-instance fixtures through mocked caldav → writer receives instance-appropriate calls (SYNC-07)
  </behavior>
  <action>
    Create `test/integration/sync-cycle.test.ts` importing runSyncCycle — expect missing module until Task 2.

    Use vi.fn mocks for CalDavReader and CalendarWriter; use openMappingStore(':memory:') and sync state store from 03-03.

    Load ICS via loadFixture for internal/public tiers and sensitive drop case.

    Create `test/integration/sync-recurrence.test.ts` for recurrence fixture names weekly-series, modified-instance, deleted-instance per pipeline-fixtures pattern.

    Commit RED. Verify tests fail.
  </action>
  <verify>
    <automated>npm test -- test/integration/; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Integration tests use mocks only — no live CalDAV/Google (D-19, TEST-05)
    - Cases cover idempotency, SYNC-06 update propagation, D-10 tier in-place update, drop cancel, delete cancel, OPS-03 last_success
    - Recurrence file references modified-instance and deleted-instance fixtures
  </acceptance_criteria>
  <done>RED: integration acceptance suite</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement runSyncCycle orchestration</name>
  <read_first>
    test/integration/sync-cycle.test.ts
    test/integration/sync-recurrence.test.ts
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-PATTERNS.md (run-sync-cycle section)
    src/domain/process-source-event.ts
  </read_first>
  <files>src/sync/run-sync-cycle.ts</files>
  <action>
    Implement `runSyncCycle(deps: SyncCycleDeps): Promise<SyncCycleResult>`:
    - deps.caldav.poll() → process changed ICS via parseIcsToSourceEvents from adapters/ical
    - For each SourceEvent: processSourceEvent; if propagation drop and mapping active → writer.cancel + markCancelled (D-09); if drop and no mapping → skip; if outbound → lookup mapping → upsertOutbound with bridge_uuid from store
    - For each deleted tombstone with uid: resolve mapping by uid/recurrenceId → cancel if active (href-only entries must not reach orchestrator — reader resolves per 03-04)
    - Per-event try/catch: log uid/recurrenceId error, increment errors, continue (D-18)
    - On cycle completion without fatal error: recordSuccess on sync state with ISO timestamp; return aggregate counts

    No process.env, tsdav, or googleapis in this file.

    Run integration tests until green.
  </action>
  <verify>
    <automated>npm test -- test/integration/ && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All integration tests pass
    - Orchestrator imports only ports and domain/adapters/ical
    - SYNC-05 drop path cancels mapped Google events
  </acceptance_criteria>
  <done>GREEN: runSyncCycle passes acceptance tests</done>
</task>

<task type="auto">
  <name>Task 3: CLI wiring, structured logging, and phase validation gate</name>
  <read_first>
    src/index.ts
    src/config/env.ts
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-CONTEXT.md (D-16, D-17)
    .planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-VALIDATION.md
  </read_first>
  <files>src/index.ts, README.md</files>
  <action>
    Extend `src/index.ts`:
    - loadConfig() at startup; exit code 1 on invalid env
    - Parse argv: `sync` subcommand; `--watch` flag loops with SYNC_INTERVAL_SECONDS (setInterval or sleep loop)
    - Wire pino logger with redact paths for MAILBOX_APP_PASSWORD, GOOGLE_REFRESH_TOKEN, GOOGLE_CLIENT_SECRET
    - Instantiate real caldav reader, mapping store, google writer, call runSyncCycle once or on interval
    - Log cycle summary counts and last_success_at from sync state (OPS-03)
    - Preserve export function main() and fileURLToPath isMain guard

    Update README with "Operator pilot" section: required env vars, run `npm run build` or `tsx src/index.ts sync`, watch mode, link to SYNC-13 horizon section from 03-04.

    Run full validation: npm test && npm run typecheck && npm run lint.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - index.ts supports sync and sync --watch per D-17
    - Logger redacts secrets
    - Full CI-equivalent commands pass locally
    - README documents pilot run steps
  </acceptance_criteria>
  <done>CLI pilot entrypoint and phase gate green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI process | Holds all member credentials (SEC-04 single account) |
| Logs → operator | Must not leak secrets or withheld event PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information disclosure | pino logs | mitigate | Redact password and OAuth tokens; log uid only at info |
| T-03-08 | Information disclosure | SYNC-05 drop | mitigate | processSourceEvent + writer cancel path tested |
| T-03-09 | Tampering | sync loop | mitigate | Read-only CalDAV; no source mutation |
| T-03-10 | Denial of service | per-event errors | mitigate | D-18 isolate failures; continue cycle |
</threat_model>

<verification>
npm test && npm run typecheck && npm run lint && npm run build
</verification>

<success_criteria>
Mocked integration suite passes; operator can run sync/sync --watch with real env; OPS-03 cycle metadata persisted.
</success_criteria>

<output>
Create `.planning/phases/03-caldav-read-uid-store-and-google-sync-loop/03-06-SUMMARY.md` when done
</output>
