# Optional overlay when NOTIFY_OWNER_EMAIL is set (withhold SMTP). Deploy:
#   docker compose -f compose.yaml -f compose.notify.yaml up -d
services:
  bridge-serhiy:
    secrets:
      - serhiy_smtp_password

secrets:
  serhiy_smtp_password:
    file: ./secrets/serhiy/smtp_password
