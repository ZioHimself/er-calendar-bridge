# Repo-root .env — Compose ${IMAGE} substitution only.
IMAGE=ghcr.io/europeanresolve/er-calendar-bridge:0.1.5

# Per-service config:
#   cp .env.bridge-serhiy-personal.example .env.bridge-serhiy-personal
# Credentials: secrets/serhiy/* — see secrets/serhiy/README.md
